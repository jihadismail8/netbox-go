# `CW1-V1-02-I4` implementation handoff — password change and session rotation

## Assignment

| Field                  | Value                                                                                              |
| ---------------------- | -------------------------------------------------------------------------------------------------- |
| Roadmap goal           | `CW1-V1-02`                                                                                        |
| Active increment       | `CW1-V1-02-I4`                                                                                     |
| Intended executor      | Codex GPT-5.6 Sol                                                                                  |
| State                  | Active `in-progress`; pre-governance local ladder green; exact-byte retention pending              |
| Working branch         | `work/cw1-v1-02-i4`                                                                                |
| Capability Profile     | `core-workflow-v1`                                                                                 |
| Capability             | Password-change credential provenance and browser-session rotation/revocation                      |
| Compatibility class    | Classified REST/gRPC identity extension informed by pinned NetBox/Django browser-session semantics |
| Required retained gate | `CW1-G00`, `CW1-V1-01`, `CW1-V1-02-I1`, `CW1-V1-02-I2`, and accepted/done `CW1-V1-02-I3`           |
| Starting revision      | `a30a60b2cbc2445173fcd92ecbcd8b40f5370674` (historical authoring base)                             |
| Starting source digest | `source-v2:sha256:bcb4ef437cd6373abb56cb93980df61234b1a6d9cf10f610fb02ad0f8c9c2f97` (historical)   |
| Starting owned entries | 3,007 (historical)                                                                                 |
| Accepted I3 base       | `8e59b3dff6ee86f8b1195d389a2b5c11211ea160`                                                         |
| Accepted I3 digest     | `source-v2:sha256:f98db5a0e1beb0590ad8eac6be5dbdcc0c6a962c85f592a005bfe8d448848b6d` / 3,007        |
| Rebased I4 tip         | `1aa112f2d27fd9d6b0c8c963a597664681b8fc61` before activation-governance edits                      |
| Pre-governance digest  | `source-v2:sha256:50c9ed75a76f5f3b387e6413c59557259a6445962ba8886db1a833f5e7afc96e` / 3,013        |
| Local-gate digest      | `source-v2:sha256:e37353f2c10db82b81cfd7623f8741a83da8a9bd2a70677a6fdcdba4ce4b5927` / 3,013        |
| Local-gate manifest    | SHA-256 `0b3d2c936171d54ad2ebdc5de7fce9d55c4383955831cb9374876a5b933fdc2c`                         |

At historical authoring entry, `work/cw1-v1-02-i4`, local `main`, and
`origin/main` all pointed to `a30a60b2cbc2445173fcd92ecbcd8b40f5370674`,
and the worktree was clean. The branch was intentionally isolated while I3
closeout remained open. Those starting revision, digest, and entry-count facts
remain part of the audit trail; they are not the active I4 base.

## Activation record — 2026-08-19

I3 is accepted and done. Its final receipt head is
`8e59b3dff6ee86f8b1195d389a2b5c11211ea160`, with accepted source digest
`source-v2:sha256:f98db5a0e1beb0590ad8eac6be5dbdcc0c6a962c85f592a005bfe8d448848b6d`
and 3,007 owned entries. Repository CI run
[32256569699](https://github.com/jihadismail8/netbox-go/actions/runs/32256569699),
job
[96079530716](https://github.com/jihadismail8/netbox-go/actions/runs/32256569699/job/96079530716),
succeeded for that final receipt revision.

The four I4 commits were rebased without silent conflict resolution onto that
accepted head. The durable old-to-new mapping is:

| Pre-rebase commit                          | Rebased commit                             | Purpose                              |
| ------------------------------------------ | ------------------------------------------ | ------------------------------------ |
| `08f31f957eeac05b6e4bd51cb697cc01172ab5e5` | `ba073ec3b5a2cd591be5bfe36144e651713d0c15` | Claim the password-session increment |
| `039d8a923b0192e8a412aa8711cc2fdb181eb907` | `900dbd901f8bd09c35b65f9401e460968e1c45b0` | Add the red-first shell and matrices |
| `fd05715310f46926511a7e3335effd96b71e2c3f` | `923b511843461d904e8dd5bdccd63d72d619f519` | Clarify collision handling           |
| `52bd9aeff24202ff5f25476316e8c9283021484f` | `1aa112f2d27fd9d6b0c8c963a597664681b8fc61` | Implement password-session rotation  |

The exact comparison command was:

```bash
GIT_GRAFT_FILE=/dev/null git --no-replace-objects range-diff \
  a30a60b2cbc2445173fcd92ecbcd8b40f5370674..52bd9aeff24202ff5f25476316e8c9283021484f \
  8e59b3dff6ee86f8b1195d389a2b5c11211ea160..1aa112f2d27fd9d6b0c8c963a597664681b8fc61
```

It reported the four commits as exact equivalents in the same order.

Immediately before these activation-governance edits, the rebased tip had
source digest
`source-v2:sha256:50c9ed75a76f5f3b387e6413c59557259a6445962ba8886db1a833f5e7afc96e`
and 3,013 owned entries. Every pre-rebase test result remains historical and
unretained. Activation itself claimed no I4 outcome. The later accepted-I3
red-first and pre-governance local source-candidate proof is recorded below;
the governance-inclusive exact-byte rerun, candidate commit, push, exact-SHA
CI, and claim/receipt sequence remain open.

The pinned toolchains remain Go 1.26.0, Node 24.18.0, and npm 11.16.0. This
packet does not complete `CW1-V1-02`, V1, any compatibility tier, the
Capability Profile, the rewrite, deployment, publication, or production
readiness.

## Pre-governance local source-candidate proof — 2026-08-19

The complete preliminary ladder ran on unchanged source digest
`source-v2:sha256:e37353f2c10db82b81cfd7623f8741a83da8a9bd2a70677a6fdcdba4ce4b5927`
with 3,013 entries. The complete manifest output had SHA-256
`0b3d2c936171d54ad2ebdc5de7fce9d55c4383955831cb9374876a5b933fdc2c`.
These are local pre-governance facts, not a commit, push, CI receipt, retained
artifact, evidence state, or tier result. This governance update changes the
digest and therefore requires the full exact-byte rerun declared below.

Before the ladder, an independent code audit found that two test helpers could
render credential-capable REST bodies or gRPC errors in failure diagnostics.
The two already-authorized password-change test paths were corrected to check
concealment before fixed diagnostics while preserving exact status, body,
header, and error assertions. No production behavior or assertion requirement
was weakened, and every earlier candidate digest/result was treated as
historical.

Local results on the recorded digest:

- All 12 exact top-level names were discovered. At reachable red-first
  revision `900dbd901f8bd09c35b65f9401e460968e1c45b0`, the application, REST,
  gRPC, parity, and all three real-PostgreSQL groups failed for their intended
  semantic gaps; no PostgreSQL group skipped and no credential was retained.
- All nine non-PostgreSQL focused matrices passed: application in 12.330
  seconds, REST in 5.205 seconds, gRPC in 0.593 seconds, and parity in 11.925
  seconds.
- The exact disposable loopback PostgreSQL boundary at `127.0.0.1:5433` used
  role/database `netbox_i4_20260819`. The three top-level focused tests and all
  11 subtests passed from `2026-08-19T14:33:37Z` through
  `2026-08-19T14:33:50Z`; the complete 11-package L4 command passed from
  `2026-08-19T14:33:50Z` through `2026-08-19T14:34:28Z`. Cleanup verification
  reported database, role, and connection counts `0:0:0`; the non-target
  fingerprint remained
  `a1064a1af4f3fc9d9f45509781d61b7b` before and after.
- All seven affected non-race packages passed. The six affected race packages
  passed with application 238.795 seconds, REST 96.184 seconds, router 10.784
  seconds, gRPC 8.524 seconds, and parity 177.177 seconds; the PostgreSQL
  adapter package also passed inside the same declared six-package command.
- The backend gate passed after its test-owned loopback-socket command was
  rerun outside the restrictive sandbox boundary. Coverage was 12.5429%
  (36,480/290,842), above the retained 12.4001% floor, with 54 measured
  packages and one reviewed exclusion.
- Pinned root `make check` passed from `2026-08-19T14:51:17Z` through
  `2026-08-19T14:56:14Z`: 28 frontend files and 125 tests passed at 61.72%
  statement coverage; inventories remained 155 baseline REST, 123 current
  REST, 179 current gRPC, and 13 current Vue entries; all 96 traceability tests
  passed; the profile remained 13 resources, 3 interfaces, 17 scenarios, and
  293 rows; OpenAPI remained 33 paths and 86 operations; and all links were
  valid across 157 Markdown files. The existing three high-severity npm
  advisories remain open; no dependency or waiver changed.
- Scoped `gofmt`, Prettier, Node syntax, Markdown-link, diff, 17-path scope,
  mode/type, and secret checks passed. The source delta remained six additions,
  no removals, and no mode, type, rename, or symlink change.

That pre-governance candidate was not committed or pushed, no candidate CI
attests it, and no evidence artifact or ledger link is authorized by this
local result.

## Required executor preamble

- Work only inside the phase-appropriate portion of this packet's closed
  permitted-file list.
- Keep the activated candidate on `work/cw1-v1-02-i4` through the required
  post-rebase local and exact-commit gates. Merge and retention follow only the
  sequence in this packet.
- Read every complete production target and its nearest complete tests before
  editing it.
- Add all named lowest-layer regressions and prove their discovery before
  changing production behavior.
- Keep `domain.Principal` credential-free. Password-change credential
  provenance and the raw material required for in-transaction revalidation
  belong only to the ephemeral application command.
- Keep dependencies pointing transport adapter to application to PostgreSQL
  port. Gin, HTTP cookies, protobuf messages, GORM rows, SQL drivers, and global
  configuration must not cross into the application contract.
- Every accepted password change owns exactly one application transaction
  containing final user/password/session/CSRF authorization and validation,
  password persistence, old-session revocation, and any replacement-session
  insertion.
- Preserve I1 token lookup/touch classification and ordering, I2 REST/gRPC
  credential grammar and unary method safety, and every I3 session/CSRF/login/
  logout/cookie outcome.
- Treat a valid session as authoritative over Authorization. Never infer the
  winning credential from cookie or header presence after authentication.
- Use constant-time comparisons for current-hash state and CSRF digest checks.
- Sample the application clock exactly once inside an accepted password-change
  transaction; adapters and repositories must not derive its timestamps.
- Return fresh session and CSRF material only after commit and only for a
  browser-session-origin change.
- Never log, print, retain in evidence, include in a test/subtest name, or place
  in an assertion diagnostic a password, password hash, session secret/digest,
  CSRF value/digest, API token, Authorization value, Cookie header, DSN, or
  other reusable credential material.
- Preserve the current Go bcrypt/password-validation behavior without
  endorsing it as NetBox parity. Do not choose a password algorithm, policy,
  validator, rehash, or imported-hash migration in I4.
- Never alter a production table, row type, bootstrap registry, migration, or
  AutoMigrate boundary. The sole schema-object exception is the explicitly
  declared L4-only deferred constraint trigger inside its owned disposable
  test schema, installed after fixtures and removed by test cleanup.
- Never hand-edit generated/frozen output, OpenAPI, protobuf output, or a
  Capability Profile/traceability claim.
- Never weaken a test, comparator, normalizer, security control, lint rule,
  coverage floor, generated check, evidence requirement, or CI requirement to
  make the branch pass.
- Stop if a required path is outside the closed list, a schema or public wire
  change is needed, I3 changes an overlapping semantic, or a broader password
  policy/security/data decision becomes necessary.

## Authority and classification

When inputs disagree, use the authority order in the execution playbook.
Current implementation and tests describe drift and regression targets; they
are not architectural or behavioral authority.

### Authoritative project inputs

- [Project language](../../CONTEXT.md)
- [Accepted ADR index](../adr/README.md)
- [ADR 0003: unified authentication and authorization](../adr/0003-unified-authentication-and-authorization.md)
- [Compatibility contract](../COMPATIBILITY.md)
- [Architecture](../ARCHITECTURE.md)
- [Coding standards](../CODING_STANDARDS.md)
- [Whole-project execution playbook](../IMPLEMENTATION_EXECUTION_PLAYBOOK.md)
- [Implementation plan](../IMPLEMENTATION_PLAN.md)
- [Goal and dependency roadmap](../ROADMAP.md)
- [Current status](../STATUS.md)
- [Testing and external-gate policy](../TESTING.md)
- [Evidence policy](../evidence/README.md)
- [Core Workflow Profile](../../contracts/netbox/v4.4.6-post7/profiles/core-workflow-v1.yaml)
- [Identity resource contract](../../contracts/netbox/v4.4.6-post7/resources/identity.yaml)
- [Identity scenarios](../../contracts/netbox/v4.4.6-post7/scenarios/identity.yaml)
- [Identity traceability rows](../../contracts/netbox/v4.4.6-post7/traceability/core-workflow-v1.yaml)
- [I1 token foundation packet](CW1-V1-02.md) and
  [accepted evidence](../evidence/2026-08-17-core-workflow-v1-token-auth-i1-v0.md)
- [I2 token transport packet](CW1-V1-02-I2.md) and
  [accepted evidence](../evidence/2026-08-17-core-workflow-v1-token-transport-i2-v0.md)
- [I3 browser-session packet](CW1-V1-02-I3.md) and
  [accepted bounded evidence](../evidence/2026-08-17-core-workflow-v1-browser-session-i3-v0.md)
- [Versioned identity protobuf source](../../netbox-backend/api/proto/netbox/identity/v1/identity_service.proto)

The directly governing accepted rules are:

- `plan.profile.transport.shared-application-use-case`;
- `plan.profile.transport.central-error-mapping`;
- `plan.profile.mutation.single-transaction`;
- `plan.profile.authorization.authenticate-default`;
- `plan.identity.interfaces.explicit-contract`;
- `plan.identity.session.mutation-requires-csrf`;
- `plan.identity.password.change-invalidates-sessions`;
- `plan.identity.token.shared-authentication-path`;
- `plan.identity.browser.session-csrf`; and
- `plan.identity.grpc.no-cookie-equivalent`.

The applicable traceability row remains `identity-browser-grpc-row` with the
unresolved `identity-browser-grpc-row-pending` verification. Domain,
application, PostgreSQL, REST-extension, gRPC-parity, and browser dimensions
remain applicable; strict REST differential is not applicable because the
network route is a classified identity extension. I4 must not edit or promote
that row.

### Pinned NetBox and Django facts

- The pinned NetBox source revision is
  `fbb948d30e79ce657fac62994a22aca72c1770a9`.
- NetBox's
  [password-change view](../../netbox/netbox/account/views.py) validates
  `PasswordChangeForm`, saves the new password, then calls
  `update_session_auth_hash(request, form.user)`.
- NetBox pins
  [Django 5.2.8](../../netbox/requirements.txt). The annotated Django 5.2.8
  tag resolves to commit
  `47fe39af56ecd0ad73b9c7562511015e96b91b80`.
- Django's exact
  [`update_session_auth_hash`](https://github.com/django/django/blob/47fe39af56ecd0ad73b9c7562511015e96b91b80/django/contrib/auth/__init__.py)
  implementation cycles the current request's session key and updates that
  session's authentication hash after a password change.
- Django's exact
  [`cycle_key`](https://github.com/django/django/blob/47fe39af56ecd0ad73b9c7562511015e96b91b80/django/contrib/sessions/backends/base.py)
  implementation creates a new key while preserving current session data and
  deletes the old key. Other sessions retain the old authentication hash and
  fail authentication when next used.
- NetBox's default password validators are visible in
  [settings](../../netbox/netbox/netbox/settings.py) and the
  [alphanumeric validator](../../netbox/netbox/utilities/password_validation.py),
  but their adoption is not authorized by this increment.

These sources decide the browser-session semantic: the browser which changes
its password keeps an authenticated session under a rotated identifier, while
prior identifiers and sibling sessions stop authenticating. The Go identity
store has no public arbitrary-session-data or session-list surface, so eagerly
deleting all old Go session rows and inserting one replacement is observably
equivalent for the declared profile to Django's current-session rotation plus
lazy rejection of sibling auth hashes.

The Go route `POST /api/auth/password/change/` and gRPC `ChangePassword` RPC are
classified extension interfaces, not NetBox's HTML redirect response. Their
existing 204 and empty-protobuf shapes stay unchanged. Token-origin password
change has no direct Django browser equivalent; its outcome is fixed by the
accepted session-invalidation rule and the explicit absence of a cookie-shaped
gRPC equivalent.

### Fixed I4 extension policy

- A browser-session-origin REST password change rotates the originating
  browser into exactly one fresh I3-shaped session, revokes every old session
  for that user including the originating and sibling rows, and returns fresh
  `sessionid` and `csrftoken` cookies after commit.
- A REST Token-origin password change revokes every browser session for that
  user and returns no browser credential or cookie mutation.
- A bearer-gRPC password change has Token provenance, revokes every browser
  session, and returns the existing empty response.
- API tokens are not password sessions. Password change neither revokes nor
  rotates them; the credential which authenticated a token-origin request
  remains governed by the independent token lifecycle.
- A valid session remains authoritative over Authorization and therefore uses
  browser provenance. Only I3's permitted invalid-session fallthrough can
  result in Token provenance.
- A Token-origin request with an unknown, expired, or inactive-owner ambient
  session cookie emits no tombstone and no replacement cookie.
- A fresh browser replacement uses the accepted I3 fixed, non-sliding 12-hour
  lifetime. This packet does not reopen I3's session-lifetime policy.

No new owner decision remains for that bounded policy. Any attempt to settle
NetBox password validators, Django/PBKDF2 compatibility, bcrypt input limits,
hash versioning, rehash-on-login, imported hashes, administrator recovery, or
CLI policy is a separate hard-to-reverse security/data decision and a stop
condition here.

## Outcome

Deliver one coherent password-change vertical slice:

1. The application receives a sealed, transport-neutral password-change
   credential provenance value and never infers it from a Principal or ambient
   input.
2. Current-password mismatch stays a stable field validation failure, while a
   malformed verifier and infrastructure failures remain Internal.
3. Every preverified change revalidates user, password-hash state, and—when
   browser-origin—session owner, expiry, and CSRF inside one application-owned
   transaction.
4. Browser origin updates the password, deletes all old sessions, creates one
   fresh replacement, and returns replacement material only after commit.
5. REST Token and gRPC bearer origin update the password and delete all browser
   sessions without creating or returning a browser replacement.
6. REST preserves 204 and emits exactly the cookie behavior declared below;
   gRPC preserves its empty response and has semantic state/error parity.
7. PostgreSQL proof covers real update/delete/create rollback and a
   deterministically coordinated concurrent-change race under the existing
   advisory transaction lock.

I4 does not complete the parent credential matrix. Throttles, trusted-proxy
source resolution, password-policy/hash lifecycle, gRPC streaming,
Origin/HTTPS-Referer/masked-CSRF policy, aggregate all-transport evidence, and
browser T4 remain open.

## Exact application contract

### Password-change credential provenance and public use-case shape

Use this exact typed application shape in
`internal/application/identity/service.go`:

```go
type PasswordChangeCredential interface {
    String() string
    GoString() string
    Format(fmt.State, rune)
    MarshalJSON() ([]byte, error)
    passwordChangeCredential()
}

func BrowserSessionPasswordChangeCredential(
    sessionSecret, csrf string,
) PasswordChangeCredential

func APITokenPasswordChangeCredential() PasswordChangeCredential

type ChangePasswordInput interface {
    String() string
    GoString() string
    Format(fmt.State, rune)
    MarshalJSON() ([]byte, error)
    passwordChangeInput()
}

func NewPasswordChangeInput(
    currentPassword, newPassword string,
    credential PasswordChangeCredential,
) ChangePasswordInput

type ChangePasswordResult interface {
    String() string
    GoString() string
    Format(fmt.State, rune)
    MarshalJSON() ([]byte, error)
    BrowserSession() (domain.BrowserSession, bool)
    passwordChangeResult()
}

type ServiceOption interface {
    apply(*Service)
}

func WithPasswordChangeEntropy(io.Reader) ServiceOption

func NewService(
    store Store,
    clock Clock,
    options ...ServiceOption,
) *Service

func (s *Service) ChangePassword(
    ctx context.Context,
    principal domain.Principal,
    input ChangePasswordInput,
) (ChangePasswordResult, error)
```

Requirements:

- Each exported application type above is a sealed interface. Its unexported
  marker prevents transport packages from implementing it.
- Each constructor returns a non-nil pointer to one private concrete type for
  that interface. Browser and Token credential constructors use the same
  private concrete credential type, so `%T` cannot reveal the winning kind.
- The private credential kind and all contained fields remain inaccessible;
  callers use only the two constructors. A nil interface, typed-nil private
  pointer, or package-internal unknown/inconsistent kind fails closed.
- A constructor-created browser credential remains valid provenance when its
  session secret is empty; it enters the transaction and receives I3's typed
  session Missing result. It is not the nil/unknown-provenance case.
- Token provenance carries no reusable token secret and cannot produce a
  browser result.
- Browser provenance carries the exact session secret and CSRF candidate only
  for request-lifetime in-transaction revalidation. It is never persisted,
  returned as input, or deliberately logged.
- The private concrete input contains the current password, new password, and
  credential. Callers use `NewPasswordChangeInput`; a transport cannot
  construct a raw-map, struct literal, or tagged DTO at the application
  boundary.
- The Service type-checks the input interface before reading it. A nil,
  typed-nil, or package-internal unknown input implementation is an Internal
  application-wiring failure with a nil result and zero lookup, transaction, or
  write; it must never panic.
- The private concrete result contains the optional browser session. REST uses
  only the typed `BrowserSession() (domain.BrowserSession, bool)` accessor after
  success; Token origin reports `false` and the zero session value, while every
  failure returns a nil result interface.
- Credential, input, and result `String`, `GoString`, and `Format` methods return
  fixed type-specific redacted literals containing no kind, presence, password,
  secret, CSRF, hash, user, or other caller value. `Format` ignores every verb,
  flag, width, and precision and never delegates to recursive field formatting.
- Go `fmt` bypasses `Formatter` for `%p` and `%T`; the pointer-only private
  representation makes those cases address-only and type-only. `%T` is the same
  private concrete credential type for both credential kinds. No secret-bearing
  concrete value is ever stored directly in an exported value type.
- Credential, input, and result `MarshalJSON` methods always return nil bytes
  plus a generic non-secret error. Accidental `encoding/json` serialization
  must fail rather than emit `{}` or an exported nested session.
- The private concrete types have no JSON, protobuf, GORM, or HTTP tags,
  implement the safe fixed-redaction `fmt.Formatter` above, and implement no
  value-revealing text, binary, marshal, or logging method.
- `domain.Principal` remains unchanged and credential-free.
- `BrowserSession()` reports present only after a committed browser-origin
  change. Token-origin success reports absent; failure returns a nil result.
- Existing direct application callers migrate explicitly to one constructor;
  no compatibility overload accepting two raw password strings is retained.

`NewService` remains source-compatible through its variadic option. With no
option, password-change replacement entropy comes from `crypto/rand.Reader`.
`WithPasswordChangeEntropy` accepts one non-nil `io.Reader`, stores it on that
Service instance, and affects only password-change replacement generation; it
does not mutate package globals or alter login/token entropy. The implementation
uses `io.ReadFull` to obtain exactly 32 bytes and returns Internal on a short
read or reader error. Production composition passes no option. Tests may pass a
private failing reader to prove replacement-generation failure after password
hashing and final transactional revalidation without global mutation.

Changing the method signature makes the red shell otherwise uncompilable. At
the red-first shell/test revision, the executor may make only these compile-only
caller migrations in already permitted paths:

- the existing gRPC caller and direct integration-test caller construct Token
  provenance and the typed input;
- the existing REST caller supplies a nil credential interface until its private
  winning-provenance context is implemented; and
- the fail-closed `ChangePassword` shell performs no write and returns only a
  generic non-secret failure.

These migrations carry no success behavior or evidence claim. The REST nil
credential is temporary red scaffolding and must be replaced by the declared
context-selected credential before any focused matrix may turn green. All
twelve named tests must compile, discover, and fail on their intended semantic
rows at that reachable red revision.

### Pre-transaction verification

The expensive bcrypt comparison and next-hash generation may happen before
the database transaction, but they are speculative and cannot authorize a
commit by themselves:

1. Type-check the input interface. Reject nil, typed-nil, or package-internal
   unknown input as Internal before reading any private field.
2. Reject an unauthenticated Principal or a nil, typed-nil, unknown, or
   inconsistent provenance value before lookup.
3. Load the user and stored current hash by `principal.ID`.
4. Map `ErrNotFound` and every infrastructure/decode error to Internal, matching
   I3's orphan handling for an already authenticated Principal.
5. Run bcrypt comparison before deciding returned-ID, activity, or mismatch
   outcomes. A malformed or unsupported verifier is Internal immediately.
6. After verifier work, map an impossible returned user ID different from the
   Principal ID to Internal.
7. Reject an inactive returned user as Unauthenticated regardless of whether
   the supplied password also mismatched. This preserves I3's inactive-user
   concealment; malformed verifier still wins as Internal.
8. For an active, correctly identified user, map only
   `bcrypt.ErrMismatchedHashAndPassword` to the existing `current_password`
   validation failure.
9. Run the unchanged `HashPassword` path over the input's private new-password
   value. Do not alter its policy or outcome in I4.
10. If those checks succeed, enter exactly one `Store.Transaction`; never
    persist or return a result before it commits.

Wrong current password and unchanged new-password-policy rejection perform
zero transactions and zero mutation. Every request which passes those
speculative checks performs exactly one transaction even if final state has
changed concurrently.

### In-transaction order

Inside the existing globally serialized identity transaction, perform this
order exactly:

1. Take one UTC application-clock sample named conceptually `now`.
2. Reload the user and current password hash by the same Principal ID.
3. Map transactional `ErrNotFound`, decode/infrastructure error, or an
   impossible different returned ID to Internal; authenticated-user
   disappearance is an orphan/infrastructure condition, not a missing
   credential.
4. Compute constant-time equality between the reloaded hash and the exact hash
   verified before the transaction, but do not branch on it yet.
5. Require the reloaded user to remain active; inactivity is Unauthenticated
   even when the hash also changed.
6. For the still-active user, map a false hash comparison to the stable
   `current_password` validation failure.
7. For browser provenance only, digest and load the exact originating session;
   require that it exists, has `Expires > now`, belongs to the same user, and
   has a constant-time CSRF-digest match for the exact candidate.
8. For browser provenance only, generate a 256-bit random raw-URL session
   secret, derive its I3 CSRF value, and transactionally probe the candidate
   digest before any mutation. A collision with an originating or sibling
   session owned by this user is Internal and stops before the password update;
   a probe infrastructure failure is Internal. A collision owned by another
   user deliberately continues to the unique-insert boundary so that the real
   update/delete/insert rollback path remains proved. Construct the replacement
   record with `Created == LastSeen == now` and `Expires == now + 12h`.
9. Update the password hash and user `updated` value using `now`.
10. Delete every browser session row for the user.
11. For browser provenance only, insert exactly the one replacement record.
12. Return from the transaction callback and wait for commit.
13. Expose the replacement result only after commit succeeds.

User/hash revalidation deliberately precedes session revalidation. Therefore,
when two requests both preverify the same old password and the first commits,
the serialized loser deterministically receives the stable
`current_password` failure instead of a race-dependent Unknown-session result.

The transaction must use a fixed-time helper for session expiry rather than
calling the current clock-sampling session helper and then sampling again.
Existing non-password session methods need not change their observable
behavior. Password change samples the application clock once on every entered
transaction, including Token origin.

### PostgreSQL port

Make the narrow port change:

```go
UpdatePassword(context.Context, int64, string, time.Time) error
```

The application supplies the UTC `updated` timestamp. The PostgreSQL adapter:

- updates only existing `password_hash` and `updated` columns;
- performs no wall-clock call;
- retains `ErrNotFound` for a zero-row update;
- participates in the active transaction without nesting or committing; and
- changes no row type, column, index, table, model registry, bootstrap, or
  migration behavior.

Migrate `ResetAdministratorPassword` mechanically to sample its existing
injected application clock and pass that value through the new port signature.
Do not otherwise change reset authorization, validation, session invalidation,
transport surface, or CLI behavior.

### Exact outcome and error matrix

| State                                               | Application outcome                                                | Transaction/effects                                               |
| --------------------------------------------------- | ------------------------------------------------------------------ | ----------------------------------------------------------------- |
| Nil, typed-nil, or unknown input implementation     | Internal                                                           | zero lookup/transaction/write; nil result                         |
| Unauthenticated Principal                           | Unauthenticated                                                    | zero lookup/transaction/write                                     |
| Nil, typed-nil, unknown, or inconsistent provenance | Unauthenticated                                                    | zero lookup/transaction/write                                     |
| Initial user `ErrNotFound`                          | Internal                                                           | one user lookup; zero transaction/write                           |
| Initial user/decode infrastructure failure          | Internal                                                           | zero transaction/write                                            |
| Initial lookup returns a different user ID          | Internal                                                           | verifier work; zero transaction/write                             |
| Stored current verifier malformed/unsupported       | Internal                                                           | zero transaction/write                                            |
| Active user's current password mismatches           | Validation on `current_password`: `Current password is incorrect.` | zero transaction/write                                            |
| Initially inactive user, password match or mismatch | Unauthenticated                                                    | verifier work; zero transaction/write                             |
| New password rejected by unchanged `HashPassword`   | Existing `new_password`/Internal outcome unchanged                 | zero transaction/write                                            |
| Transactional user missing/decode/infrastructure    | Internal                                                           | one transaction; zero durable write                               |
| Transactional lookup returns a different user ID    | Internal                                                           | one transaction; zero durable write                               |
| Transactional user inactive, hash same or changed   | Unauthenticated                                                    | one transaction; zero durable write                               |
| Active transactional user's hash differs            | Validation on `current_password`: `Current password is incorrect.` | one transaction; zero durable write                               |
| Browser credential has empty session secret         | Existing typed session Missing                                     | one transaction; zero durable write                               |
| Browser session absent                              | Existing typed session Unknown                                     | one transaction; zero durable write                               |
| Browser session `Expires <= now`                    | Existing typed session Expired                                     | one transaction; zero durable write                               |
| Browser session belongs to another Principal        | Unauthenticated                                                    | one transaction; zero durable write                               |
| Browser CSRF empty or mismatched                    | Forbidden                                                          | one transaction; zero durable write                               |
| Session/user/store/decode failure                   | Internal                                                           | one transaction; zero durable write                               |
| Replacement collides with this user's old session   | Internal                                                           | one transaction; zero mutation; absent browser result             |
| Replacement-candidate lookup failure                | Internal                                                           | one transaction; zero mutation; absent browser result             |
| Entropy, update, delete, insert, or commit failure  | Internal                                                           | complete rollback; absent browser result                          |
| Browser success                                     | success with one replacement `BrowserSession`                      | password update + all-session delete + one insert commit together |
| Token success                                       | success with absent replacement                                    | password update + all-session delete commit together              |

An application/shared error returned from the transaction remains typed;
every raw repository, entropy, or commit error is wrapped as Internal. No raw
cause reaches REST or gRPC.

### Durable invariants

- Old password authentication fails after commit; the winning new password
  succeeds.
- Browser-origin success invalidates the originating old session and every
  sibling; exactly the returned replacement authenticates and accepts its
  returned CSRF value.
- Token-origin success leaves zero browser sessions.
- API-token rows, secrets, scopes, expiry, revocation, and `last_used` policy
  are unchanged. Password change itself performs no token lookup or touch
  beyond authentication already completed by its transport boundary.
- Any error after real DML leaves the old password and all preexisting sessions
  unchanged and leaves no replacement row.
- Returned secrets never survive a failed callback or commit.
- A same-user random collision is detected transactionally before mutation, so
  an originating or sibling identifier can never become the replacement.
  Another user's collision reaches the unique insert and rolls back the
  preceding password update and all-session deletion; every other replacement
  insert failure has the same rollback boundary.

## Exact REST contract

### Provenance handoff

`Handler.Middleware()` records which credential actually succeeded in an
adapter-private Gin/request context value:

- after a valid session and exact CSRF pair, record browser origin plus the
  exact session secret and CSRF candidate;
- after successful Token authentication, record Token origin; and
- never record a value before authentication/CSRF succeeds.

The password handler consumes that value and constructs the corresponding
application credential. It must not infer origin from `Cookie`,
`Authorization`, Principal fields, route, or result expectations. A missing or
invalid context value is an internal adapter-wiring failure: return the generic
HTTP 500 body, call no application service, mutate nothing, and emit no cookie.

Preserve I3 arbitration exactly:

| Presented state                                                           | Winning provenance/outcome                         |
| ------------------------------------------------------------------------- | -------------------------------------------------- |
| Valid session + valid CSRF + absent/valid/invalid/malformed Authorization | Browser session; Authorization is ignored          |
| Valid session + invalid CSRF + valid Token                                | 403; no Token fallback and no application mutation |
| Duplicate `sessionid` cookies                                             | Existing fail-closed response; no Token fallback   |
| Session infrastructure failure + any Authorization                        | Existing 500; no Token fallback                    |
| Unknown/expired/inactive-owner session + valid Token                      | Token                                              |
| No or one empty session cookie + valid Token                              | Token                                              |
| No accepted credential                                                    | Existing identity-extension 401                    |

The endpoint remains registered once at
`POST /api/auth/password/change/`. No route or middleware-order change is
permitted.

### Request, response, and cookie behavior

The request body remains:

```json
{
  "current_password": "...",
  "new_password": "..."
}
```

Malformed JSON retains the existing `non_field_errors` validation response.
Current-password failure remains HTTP 400 with the existing field shape. Typed
Unauthenticated, Forbidden, and Internal causes continue through the central
identity REST mapper as 401, 403, and 500 respectively.

Every success remains HTTP 204 with an empty body.

Browser-origin success emits exactly two issue cookies, not tombstones:

| Property | `sessionid`                      | `csrftoken`                 |
| -------- | -------------------------------- | --------------------------- |
| Value    | fresh replacement session secret | fresh I3-derived CSRF value |
| Path     | `/`                              | `/`                         |
| Domain   | omitted/host-only                | omitted/host-only           |
| HttpOnly | `true`                           | `false`                     |
| SameSite | `Lax`                            | `Lax`                       |
| Secure   | runtime production setting       | runtime production setting  |
| Max-Age  | exactly 43,200 seconds           | exactly 43,200 seconds      |
| Expires  | exact application result expiry  | omitted                     |

Additional requirements:

- The new session/CSRF pair differs from the presented pair without printing
  either value in a failure diagnostic.
- Setting the two new cookie values replaces the browser's old pair; do not
  also emit deletion tombstones.
- Token-origin success emits exactly zero `Set-Cookie` headers, even when an
  unknown, expired, or inactive-owner session cookie was present.
- Every parse, authentication, current-password, session, CSRF, persistence,
  entropy, or commit failure emits zero `Set-Cookie` headers.
- The handler must not call `clearCookies` after password change.
- Logs may contain only the safe numeric user ID and outcome, never an origin
  secret or submitted password.

## Exact gRPC and parity contract

- The unary interceptor remains the sole bearer grammar and write-scope
  authentication boundary. I4 does not edit it.
- `Server.ChangePassword` constructs `APITokenPasswordChangeCredential()` and
  calls the same application `ChangePassword` use case as REST.
- An unauthenticated context remains `codes.Unauthenticated`.
- Current-password/new-password validation remains `codes.InvalidArgument`;
  Forbidden remains `codes.PermissionDenied`; infrastructure remains
  `codes.Internal` through the central mapper.
- Success returns the existing empty `ChangePasswordResponse` and creates no
  browser session.
- No protobuf or generated change is required or permitted.
- REST Token and gRPC bearer rows must have the same Principal, application
  reason, password state, all-session revocation, API-token retention, and
  rollback result.
- Browser cookie issuance is REST-only transport mechanics and has no gRPC
  equivalent. Its absence neither weakens shared application semantics nor
  earns a T3 claim.

## Closed permitted-file list

The list is phase-closed. A path permitted in a later phase is not permission
to edit it during this packet-only authoring task.

### Historical packet-authoring phase

1. `docs/increments/CW1-V1-02-I4.md` — new; the only path permitted when the
   packet was first authored

### Implementation and tests on the activated branch

2. `netbox-backend/internal/application/identity/service.go`
3. `netbox-backend/internal/application/identity/password_change_matrix_test.go`
   — new
4. `netbox-backend/internal/application/identity/password_change_postgres_test.go`
   — new
5. `netbox-backend/internal/application/identity/service_integration_test.go`
   — update only the existing direct `ChangePassword` call to the exact typed
   Token-provenance input/result shape; no other assertion or behavior change
6. `netbox-backend/internal/adapters/postgres/identity/store.go`
7. `netbox-backend/internal/adapters/rest/netbox/identity/http.go`
8. `netbox-backend/internal/adapters/rest/netbox/identity/password_change_test.go`
   — new
9. `netbox-backend/internal/adapters/grpc/identity/server.go`
10. `netbox-backend/internal/adapters/grpc/identity/password_change_test.go` —
    new
11. `netbox-backend/test/parity/identity_password_change_test.go` — new

### Source-included activation, evidence, and done governance

12. `docs/increments/CW1-V1-02-I4.md`
13. `docs/increments/README.md`
14. `docs/ROADMAP.md`
15. `docs/STATUS.md`
16. `docs/TESTING.md`
17. `docs/IMPLEMENTATION_PLAN.md` — clarify only that browser origin rotates
    one current session while Token origin revokes all browser sessions; do
    not declare a hash/validator policy
18. `AGENTS.md`

These seven paths may record only the current active `in-progress` activation
state now. After candidate exact-SHA CI, all seven are the source-included
portion of the separately reviewed claim-only `in-progress -> evidence`
attestation. After later project-owner acceptance, all seven are again the
source-included ceiling for the distinct claim-only `evidence -> done`
transition. Neither transition may change behavior, security policy, contract,
traceability, tier, parent state, or another goal.

### Source-digest-excluded evidence and receipts

19. `docs/evidence/2026-08-19-core-workflow-v1-password-session-i4-v0.md` —
    new
20. `docs/evidence/README.md`

The reviewed `in-progress -> evidence` transition must touch exactly all nine
paths 12 through 20 together: the seven source-included governance paths plus
these two source-digest-excluded evidence paths. An evidence-only two-path
receipt cannot change I4 state. The later owner-accepted `evidence -> done`
transition is a separate nine-path claim/receipt sequence under the same path
ceiling.

No other path is permitted. A required compile, test, governance, or evidence
change outside this list is a stop-and-amend condition. In particular, the
existing I1/I2/I3 packets and artifacts are immutable to I4.

## Forbidden scope

- No change to `domain.Principal`, domain user/session/token types, or general
  authentication context contracts.
- No profile, resource, scenario, traceability, verification-set, inventory,
  normalizer, comparator, fixture, or compatibility-oracle change.
- No REST route, router-production, OpenAPI, protobuf source, generated Go,
  frozen legacy, dependency, or toolchain change.
- No production database row, schema, constraint, index, table, model registry,
  bootstrap, migration, or AutoMigrate change. The sole exception is the
  packet-declared test-only deferred constraint trigger in the disposable L4
  schema; it is never registered, migrated, or retained.
- No token parser, token failure kind, lookup/touch ordering, `last_used`,
  source-IP, write-scope, token-management, or gRPC interceptor/stream change.
- No I3 login, logout, CSRF derivation/recovery, method classification,
  fallback, session lifetime, cookie name, or cookie-flag change except the
  adapter-private provenance handoff and password-change result handling
  explicitly listed here.
- No login/token throttle, trusted-proxy, forwarded-header, CORS,
  Origin/Referer, Django masked-CSRF, TLS, stale-cookie cleanup, or gRPC stream
  behavior.
- No password algorithm, cost, length-unit, maximum, character-class,
  validator, Unicode, PBKDF2/Django hash, bcrypt input-limit, version marker,
  rehash, upgrade, import, reset-policy, or recovery-policy decision.
- No change to `HashPassword`, `passwordCost`, current password validation text,
  or existing new-password validation/error behavior.
- No administrator authorization or CLI behavior change beyond the mechanical
  `UpdatePassword(..., changedAt)` port migration in the already permitted
  application file.
- No RBAC, DCIM, IPAM, changelog, Vue, deployment, production, or legacy-stack
  work.
- No L5 differential claim, L6/T3 promotion, L8/T4 claim, tier/profile
  promotion, parent completion, publication, deployment-readiness,
  production-readiness, or full-rewrite claim.

## Exact red-first tests

Add these top-level tests before production behavior changes:

```text
TestPasswordChangeCredentialProvenanceMatrix
TestPasswordChangeVerificationClassification
TestPasswordChangeSessionRotationAndInvalidation
TestPasswordChangeTransactionRollbackAndStateRevalidation
TestRESTPasswordChangeCredentialProvenance
TestRESTPasswordChangeSessionCookieRotation
TestRESTPasswordChangeFailureAndNoCookieMatrix
TestGRPCPasswordChangeUsesTokenProvenance
TestPasswordChangeRESTGRPCParity
TestPostgresPasswordChangeRotationRollback
TestPostgresConcurrentPasswordChangesHaveOneWinner
TestPostgresPasswordChangeSerializesWithLogin
```

If the new tests need the typed input/result shell to compile, add only the
sealed types, safe formatting/serialization methods, source-compatible entropy
option, fail-closed stub, and exact compile-only caller migrations declared
above. All twelve test names must then be discoverable and the intended
semantic rows must be red before production implementation begins. A
compile-only failure does not prove the behavioral red matrix.

Required pre-correction red observations include:

- no password-change credential provenance or typed result exists;
- browser-session success deletes the current session instead of returning a
  replacement;
- REST emits two cookie tombstones after every success, including Token origin;
- current user/hash verification occurs outside the transaction without final
  revalidation;
- browser session owner/expiry/CSRF are not revalidated in the mutation
  transaction;
- missing, infrastructure, and malformed-verifier failures collapse into the
  `current_password` validation result;
- the repository samples wall time for the user update;
- two preverified concurrent changes can both commit with the last writer
  winning; and
- no rollback proof covers password update followed by real all-session delete
  and replacement insert; and
- no real-PostgreSQL proof serializes password-change replacement against login
  in both advisory-lock orders.

### Application matrices

`TestPasswordChangeCredentialProvenanceMatrix` covers:

- unauthenticated Principal and nil/typed-nil/unknown provenance with zero
  lookup;
- Token provenance with no browser result;
- browser provenance with exactly one committed result;
- empty session secret, owner mismatch, empty/mismatched CSRF, and valid
  session/CSRF;
- result kind independent of ambient transport fields; and
- fixed redaction for string and non-string verbs, including `%v`, `%+v`,
  `%#v`, `%s`, `%q`, `%x`, `%X`, `%d`, `%o`, `%b`, `%c`, `%U`, `%e`, `%f`,
  `%g`, and `%t`, plus representative flags, widths, and precisions over
  credential, input, and result values, with no submitted or generated value
  in output;
- `%p` over each interface's dynamic private pointer and `%T` over each interface
  produce only an address/type, never a recursively formatted field; both
  credential constructors have the same `%T` result; and a scoped source audit
  finds no non-pointer concrete credential/input/result value or `%p` logging
  callsite;
- JSON marshal rejection with nil output for credential, input, and result; and
- a scoped source/log/assertion audit proving implementation call sites do not
  pass raw contained values to logging, assertion diagnostics, or retained
  output.

`TestPasswordChangeVerificationClassification` covers:

- nil, typed-nil, and package-internal unknown input implementations as
  Internal with a nil result, no panic, and zero lookup/transaction/write;
- initial user missing, infrastructure, wrong returned ID, inactive with both a
  matching and mismatching password, active current mismatch, malformed hash,
  inactive malformed hash, and successful verifier paths;
- unchanged new-password validation paths without broadening their assertions;
- transactional user missing/Internal, wrong returned ID/Internal,
  inactive-with-same-or-changed-hash/Unauthenticated, and
  active-hash-changed/current-password states;
- exact reason, field, description, lookup count/order, transaction count, and
  zero mutation; and
- raw causes retained internally but sanitized at public boundaries.

`TestPasswordChangeSessionRotationAndInvalidation` covers:

- originating plus multiple sibling sessions;
- browser success deleting every old row and leaving exactly one new row;
- new secret/CSRF inequality through constant-time boolean assertions;
- new record `Created`, `LastSeen`, `Expires`, user, and one-clock behavior;
- replacement authentication and CSRF success;
- old-session authentication failure;
- Token success leaving zero sessions and an absent browser result; and
- independent API token retention.

`TestPasswordChangeTransactionRollbackAndStateRevalidation` covers:

- exact transaction event order;
- default entropy with no Service option and injected short-read/error entropy
  through `WithPasswordChangeEntropy`, proving failure occurs only after initial
  hashing and final in-transaction user/session/CSRF revalidation;
- originating/sibling replacement collisions and candidate-lookup
  infrastructure failure before mutation, while an other-user collision still
  reaches the real unique-insert rollback boundary;
- update, delete, insert, entropy, and post-DML callback/finalization failures;
- rollback to the old password and complete prior-session set;
- absent returned browser material for every failure;
- final hash revalidation before any session check/write; and
- no adapter/repository clock sampling.

### REST matrices

`TestRESTPasswordChangeCredentialProvenance` covers valid-session-first
arbitration, session-CSRF refusal without Token fallback, every I3-permitted
invalid-session fallthrough to Token, duplicate-cookie and infrastructure
containment, absent context failure, and exact selected Principal/origin.

`TestRESTPasswordChangeSessionCookieRotation` covers 204/empty response,
exactly two fresh issue cookies for browser origin, all cookie attributes,
fixed lifetime, no tombstones, replacement usability, old/sibling rejection,
and exactly zero cookies for Token origin with or without an ambient stale
session cookie.

`TestRESTPasswordChangeFailureAndNoCookieMatrix` covers malformed JSON,
current/new-password validation, unauthenticated, session typed causes,
Forbidden CSRF, infrastructure, entropy, DML, and post-DML transaction failure.
Every row asserts its exact status/body, zero handler leakage, and zero
`Set-Cookie`.

### gRPC and parity matrices

`TestGRPCPasswordChangeUsesTokenProvenance` covers missing Principal,
validation and Internal mapping, empty response, absent browser result, complete
browser-session invalidation, no token revocation, and no proto/generated
change.

`TestPasswordChangeRESTGRPCParity` compares REST Token and bearer-gRPC using
the same user state. It asserts the same Principal, application reason,
current/new password outcome, committed session state, token retention,
rollback result, and no extra application token lookup/touch. It separately
records browser cookie issuance as REST-only mechanics without treating it as
a gRPC mismatch.

No test may compare or print a reusable credential value in a failure message.
Use fixed-length digest equality, constant-time boolean comparisons, row
counts, and generic diagnostics.

## Real-PostgreSQL proof

Use `newCredentialPostgres`, the owned disposable-schema cleanup, silent GORM
logging, `sessionPostgresPinnedStore`, and the existing `pg_locks`-based
advisory-wait helper. SQLite, a mock, an in-memory store, or a timing-only race
cannot substitute for L4.

### `TestPostgresPasswordChangeRotationRollback`

Use real user and session rows and inject failures around real DML. Required
subtests:

1. failure after real password update rolls the hash/timestamp back and keeps
   every old session;
2. failure after real all-session deletion rolls the password and deleted rows
   back;
3. browser replacement insert failure rolls back the password update and all
   deletions and returns an absent result;
4. a real PostgreSQL COMMIT failure after successful browser-origin
   update/delete/create returns an absent result and leaves old
   password/sessions intact;
5. Token-origin actual COMMIT failure has the same rollback without a
   replacement attempt; and
6. successful browser and Token rows prove the exact final invariants.

The update/delete/insert injection wrappers must participate inside the actual
PostgreSQL transaction. A failure before real DML does not prove rollback.

For the two actual COMMIT-failure rows, install a schema-local test function
and a `DEFERRABLE INITIALLY DEFERRED` constraint trigger only after fixtures
exist:

```sql
CREATE FUNCTION i4_fail_password_change_commit()
RETURNS trigger
LANGUAGE plpgsql
AS $$
BEGIN
  RAISE EXCEPTION 'injected deferred password-change commit failure';
END;
$$;

CREATE CONSTRAINT TRIGGER i4_password_change_commit_failure
AFTER UPDATE OF password_hash ON go_identity_users
DEFERRABLE INITIALLY DEFERRED
FOR EACH ROW
EXECUTE FUNCTION i4_fail_password_change_commit();
```

Because the trigger fires only when PostgreSQL resolves deferred events at
COMMIT, the application callback must have completed all real DML before the
driver receives the commit error. Assert absent returned browser material and
query from a fresh connection to prove password, timestamp, and session rows
rolled back. Install/drop the trigger and function in the disposable test
schema around each browser/Token subtest using cleanup that also runs after a
failed commit. Do not use a callback error and label it a COMMIT failure, do
not alter production bootstrap/schema code, and do not retain the trigger.

### `TestPostgresConcurrentPasswordChangesHaveOneWinner`

Use two independently pinned PostgreSQL connections and explicit channels; no
`time.Sleep`, unobserved delay, or scheduler hope is permitted.

Required browser-origin sequence:

1. Create one active user, two independent browser sessions, and at least one
   usable API token.
2. Start two password changes with the same old password, distinct new
   passwords, and the two session credentials.
3. Block both calls at transaction entry only after both have completed their
   initial bcrypt verification.
4. Release the selected first call to obtain the real identity advisory lock
   and hold it before commit.
5. Release the second call on its separate connection and query `pg_locks`
   until that exact backend is observed waiting for the advisory lock.
6. Release the first commit, then allow the second transaction to enter and
   perform final hash revalidation.
7. Require exactly one success and one stable `current_password` validation
   failure.
8. Require exactly one surviving replacement session; both old sessions are
   rejected, and the winner's replacement authenticates with its CSRF.
9. Require only the winning new password to authenticate; the old and losing
   new passwords fail.
10. Require the preexisting API token to remain usable.

Add a Token-origin subtest using the same no-sleep barrier if it can remain
inside the named top-level test: exactly one change wins, the loser receives
the same stale-current-password classification, and zero browser sessions
remain. The browser row is mandatory; the Token concurrency subtest is
mandatory unless a packet amendment explains why the same transaction path is
already exhaustively proved.

All coordination comes from channels and observed PostgreSQL lock state.
Bounded test timeouts may fail a deadlock but must not establish ordering. Test
output and `-v` names remain credential-free.

### `TestPostgresPasswordChangeSerializesWithLogin`

This is mandatory because password change creates a replacement session while
login independently creates a fresh session. I3's reset-wins login regression
and I4's two-password-change race do not prove the opposite lock order or the
final session set when both successful use cases can return material.

Use two pinned connections, explicit preverification barriers, and observed
`pg_locks` contention. Run both orders without sleeps:

1. **Password change wins:** both operations complete their initial password
   verification; password change acquires the advisory lock and is held before
   commit; login is observed waiting; password change commits; login enters,
   rejects the stale verified hash as Unauthenticated, and returns no session.
   Exactly the password-change replacement survives.
2. **Login wins:** both operations complete initial verification; login
   acquires the advisory lock and is held before commit; password change is
   observed waiting; login commits one fresh session; password change then
   revalidates the still-current hash, commits, deletes the old and just-created
   login sessions, and creates its one replacement. Both calls may report
   success at their serialization points, but the returned login session must
   fail afterward and exactly the password-change replacement survives.

Both orders require the winning final password, rejected old password, exact
one-row session state, replacement CSRF validity, no reusable secret output,
and unchanged API-token usability.

## Activated execution sequence

1. Verify the accepted I3 base, rebased four-commit order, exact range-diff,
   clean branch state, current source digest/count, and pinned toolchains.
2. Re-read every complete permitted production target and nearest complete
   test at the accepted-I3-based revisions; reconcile any overlap against the
   governing authority without silently changing the packet.
3. In a disposable worktree at the reachable rebased red-first shell/test
   revision `900dbd901f8bd09c35b65f9401e460968e1c45b0`, prove all twelve exact
   top-level tests are discovered and fail for the intended semantic gaps.
4. Return to the rebased implementation tip and run the focused application,
   REST, gRPC, and parity matrices green without changing their assertions or
   I3 arbitration.
5. Run the I1/I2/I3 regressions, every affected package, and the affected race
   command.
6. Run all three focused real-PostgreSQL tests and the complete 11-package L4
   command against one owned disposable PostgreSQL boundary.
7. Assemble only the seven permitted source-included governance paths. They
   record activation and the bounded candidate state, not a retained result.
8. Run backend, root, documentation, link, diff, secret, digest, and manifest
   gates on those exact unchanged local candidate bytes.
9. Commit the exact local candidate, re-run every mandatory exact-commit gate,
   and prove the source digest and manifest are unchanged.
10. Push only after the local and exact-commit ladders are green, require
    repository CI for that exact SHA, and then follow the reviewed nine-path
    claim-only evidence transition below.

## Command ladder

Run Go commands from `netbox-backend` with the pinned Go toolchain and a
disposable cache. Prove discovery before focused execution; an anchored
zero-test run is not a pass.

### Test discovery

```bash
go test ./internal/application/identity \
  -list '^Test(PasswordChange|PostgresPasswordChange|PostgresConcurrentPasswordChanges)' \
  -count=1

go test ./internal/adapters/rest/netbox/identity \
  -list '^TestRESTPasswordChange' \
  -count=1

go test ./internal/adapters/grpc/identity \
  -list '^TestGRPCPasswordChange' \
  -count=1

go test ./test/parity \
  -list '^TestPasswordChangeRESTGRPCParity$' \
  -count=1
```

The output must contain all twelve exact top-level names listed by this packet.

### Focused application

```bash
go test ./internal/application/identity \
  -run '^(TestPasswordChangeCredentialProvenanceMatrix|TestPasswordChangeVerificationClassification|TestPasswordChangeSessionRotationAndInvalidation|TestPasswordChangeTransactionRollbackAndStateRevalidation)$' \
  -count=1
```

### Focused REST

```bash
go test ./internal/adapters/rest/netbox/identity \
  -run '^(TestRESTPasswordChangeCredentialProvenance|TestRESTPasswordChangeSessionCookieRotation|TestRESTPasswordChangeFailureAndNoCookieMatrix)$' \
  -count=1
```

### Focused gRPC and parity

```bash
go test ./internal/adapters/grpc/identity \
  -run '^TestGRPCPasswordChangeUsesTokenProvenance$' \
  -count=1

go test ./test/parity \
  -run '^TestPasswordChangeRESTGRPCParity$' \
  -count=1
```

### Required focused PostgreSQL

```bash
NETBOX_TEST_POSTGRES_DSN='<owned-disposable-postgres-dsn>' \
  go test ./internal/application/identity \
  -run '^TestPostgres(PasswordChangeRotationRollback|ConcurrentPasswordChangesHaveOneWinner|PasswordChangeSerializesWithLogin)$' \
  -count=1 -v
```

Record only a redacted description of the disposable boundary. Assert all
three test names are discovered and that none skips.

### I1/I2/I3 and affected-package regression

```bash
go test \
  ./internal/application/identity \
  ./internal/adapters/postgres/identity \
  ./internal/adapters/rest/netbox/identity \
  ./internal/adapters/rest/netbox/router \
  ./internal/adapters/grpc/identity \
  ./test/parity \
  ./cmd/netbox_go_admin \
  -count=1

go test -race \
  ./internal/application/identity \
  ./internal/adapters/postgres/identity \
  ./internal/adapters/rest/netbox/identity \
  ./internal/adapters/rest/netbox/router \
  ./internal/adapters/grpc/identity \
  ./test/parity \
  -count=1
```

### Complete L4

```bash
NETBOX_TEST_POSTGRES_DSN='<owned-disposable-postgres-dsn>' \
  go test \
    ./internal/database \
    ./internal/adapters/postgres/bootstrap \
    ./internal/adapters/postgres/dcim \
    ./internal/adapters/postgres/dcim/row \
    ./internal/adapters/postgres/ipam \
    ./internal/adapters/postgres/ipam/row \
    ./internal/adapters/postgres/changelog \
    ./internal/application/dcim \
    ./internal/application/ipam \
    ./internal/application/identity \
    ./cmd/netbox_go_admin \
    -count=1
```

### Backend, repository, documentation, and digest gates

```bash
make -C netbox-backend check
make check
make source-digest
node scripts/check_markdown_links.mjs
git diff --check
```

Run `gofmt` only over the permitted changed Go files. Run Prettier only over
the permitted governance/evidence files in their applicable phase. Record
exact commands, UTC start/end times, exit results, toolchains, revision,
digest/count, skipped gates, and the credential-redacted PostgreSQL boundary.

## Gate disposition

- **L0:** mandatory for each changed Go/document path and exact test discovery.
- **L1:** mandatory for application, PostgreSQL adapter, REST, gRPC, router
  regression, parity, and the administrator CLI compile/regression package
  affected mechanically by the timestamp-bearing password-update port.
- **L2:** mandatory backend gate; the pinned frontend gate remains part of the
  root command even though I4 changes no frontend source.
- **L3:** mandatory full deterministic `make check` on one unchanged digest.
- **L4:** mandatory for actual PostgreSQL rollback, advisory-lock ordering, and
  concurrency. SQLite/mock results cannot satisfy it.
- **L5:** skipped/not applicable. The password endpoint is a classified
  extension, not a strict NetBox REST differential/T2 row.
- **L6:** affected local REST/gRPC semantic parity is mandatory. Compatibility
  promotion is prohibited because the identity extension has no REST T2 owner
  row and I4 does not earn T3.
- **L7:** not applicable; no deployment/configuration wiring changes.
- **L8:** skipped/deferred. Cookie mechanics remain browser-applicable, but
  real-browser T4 belongs to V5 and is not proved by `httptest`.
- **L9:** mandatory after activation. Tested-digest confirmation, exact
  candidate CI, the nine-path `in-progress -> evidence` claim attestation,
  attestation `make check` and exact-SHA CI, its digest-excluded receipt, later
  owner review, and the separate `evidence -> done` claim/receipt remain
  distinct boundaries.

## Active candidate boundary

Until the post-rebase gates and receipt sequence close, governance and reports
may say only:

- I3 is accepted and done at the exact base recorded above;
- I4 is active `in-progress` on the exact rebased four-commit series plus the
  two authorized diagnostic-safety test corrections recorded above;
- pre-rebase results are historical; the pre-governance local result is green
  but is not retained I4 evidence;
- `plan.identity.password.change-invalidates-sessions` and
  `identity-browser-grpc-row-pending` remain unresolved;
- `CW1-V1-02` remains open;
- the profile remains T1 and pre-publication; and
- no T2/T3/T4, V1, deployment, production, publication, or full-rewrite claim
  has changed.

Do not add an evidence artifact, ledger link, retained-evidence or traceability
outcome box, STATUS completion statement, verification reference, or tier
claim before the exact retention boundary permits it.

## Active candidate checklist

- [x] Accepted I3 overlap and authority are re-read before the post-rebase
      proof reruns.
- [x] Only the packet-authorized production/test paths change.
- [x] All twelve exact tests exist and discovery is nonzero.
- [x] Meaningful behavioral red results are recorded without credential output.
- [x] Sealed provenance and exact typed input/result replace the old signature.
- [x] Current verifier mismatch is distinct from malformed verifier/Internal.
- [x] Final user/hash/session/CSRF validation occurs inside one transaction.
- [x] Browser origin leaves exactly one fresh replacement and revokes all old
      sessions.
- [x] REST Token and gRPC bearer origin leave zero browser sessions and no
      replacement.
- [x] API tokens remain independent and usable.
- [x] Browser REST success emits exactly two fresh cookies; Token success and
      every failure emit none.
- [x] Update timestamp comes from the single application clock sample.
- [x] Real PostgreSQL rollback and no-sleep concurrency tests pass and do not
      skip.
- [x] I1/I2/I3 regressions, affected packages, race, backend, root, link, and
      diff checks pass on one unchanged post-rebase candidate digest.
- [x] No password-policy, schema, generated, contract, traceability, or tier
      scope changed.
- [x] Work remains on the bounded branch until the exact candidate and receipt
      sequence authorizes integration.

This checked list records the pre-governance local candidate result only. It
does not by itself authorize `evidence`, `done`, merge, or retention.

## Activation and retention conditions

The I3 acceptance, final receipt CI, clean accepted base, exact I4 rebase, and
pre-governance digest/count are recorded in the activation section above. They
activate I4 but prove no I4 behavior. Before the source candidate can be
committed and pushed:

1. Every I3-overlapping target and test must be re-read; any semantic change
   requires an explicit packet amendment.
2. The accepted-I3-based red-first proof must use the reachable red-first
   shell/test revision; pre-rebase red output cannot be relabeled.
3. All focused, regression, race, L3, L4, local parity, documentation, scope,
   secret, digest, and manifest gates must rerun without source mutation.
4. The final candidate must contain only phase-permitted paths and retain the
   parent, profile, tiers, and residual work as open.

After those activation conditions, candidate retention and the claim-only
transition to `evidence` follow this exact sequence:

1. Assemble behavior, tests, and all seven permitted source-included governance
   paths in the bounded I4 candidate. Governance records only the local result
   actually proved, keeps I4 `in-progress`, keeps the traceability row, parent,
   profile, and tiers open, and makes no post-CI factual claim.
2. Run every mandatory candidate gate, including the three focused PostgreSQL
   tests and complete L4 command, on those exact unchanged source bytes.
3. Commit the exact tested source candidate and record its reachable full SHA,
   source-v2 digest/count, complete source manifest, clean status, toolchains,
   and non-secret environment.
4. Rerun every mandatory exact-commit gate on the same digest, push the
   candidate, and require green repository CI for that exact candidate SHA.
   Candidate CI does not itself change I4 from `in-progress`.
5. After candidate CI succeeds, prepare one reviewed claim-only
   `in-progress -> evidence` transition which touches exactly the nine allowed
   paths 12 through 20. The seven source-included paths may change only the
   bounded I4 result/state wording; the two excluded paths retain the
   credential-free evidence summary, candidate-CI receipt, and ledger link.
6. With Git replacement objects and grafts disabled, reconstruct and durably
   retain the complete tested-candidate and proposed-attestation
   `make source-manifest` outputs. Derive and retain their complete manifest
   diff, including every path/kind set, normalized mode, raw byte size/hash,
   and closed symlink target. It must show only the seven allowed
   source-included governance changes, with no added, deleted, type-changed,
   mode-changed, or retargeted owned entry. Separately retain the exact Git
   nine-path old/new mode, size, and raw-byte SHA-256 claim diff, including the
   two source-digest-excluded paths and explicit old-side absence for the new
   I4 artifact.
7. Record the tested revision/digest/count, tested-manifest commitment, exact
   candidate CI provenance, complete claim-diff commitment, and proposed
   attestation digest/count in the I4 artifact without secrets or a tier
   marker. Independently review the full manifests and nine-path diff for any
   behavior, security, contract, scenario, fixture, comparator, normalizer,
   toolchain, parent, profile, or tier change.
8. On those exact proposed-attestation bytes, run `make source-digest`, retain
   the complete source manifest, run the scoped format/link/diff/secret checks,
   and run pinned `make check`. Commit exactly the nine reviewed paths only
   after every local attestation gate is green.
9. Push the claim-only attestation commit and require green repository CI for
   its exact SHA. The `in-progress -> evidence` transition becomes effective
   only after that exact-SHA CI succeeds; before then I4 remains
   `in-progress`.
10. Append the exact attestation-CI receipt only to the two excluded evidence
    paths, prove the source digest and complete manifest are unchanged, commit
    and push that receipt, and require green CI for the receipt SHA. This
    records the already-effective `evidence` state without another source edit.

Explicit project-owner semantic review begins only after the evidence receipt
above is durable. Owner acceptance then follows a separate
`evidence -> done` claim/receipt sequence:

1. Record the owner's bounded acceptance and proposed `done` wording by
   touching exactly the same seven source-included governance paths and two
   excluded evidence paths. Do not change behavior, security policy, contract,
   traceability, tier, parent state, or any other goal.
2. Reconstruct, retain, and independently review the complete evidence-state
   and proposed-done source manifests, their full derived manifest diff, and
   the exact nine-path old/new mode, size, and raw-byte SHA-256 claim diff with
   replacements and grafts disabled.
3. Compute the proposed done-attestation digest, run the scoped checks and
   pinned `make check`, commit exactly the nine paths, push, and require green
   exact-SHA CI. The `evidence -> done` transition becomes effective only after
   that CI succeeds.
4. Append the exact done-attestation CI receipt only to the two excluded
   evidence paths, prove the source digest/manifest unchanged, commit, push,
   require receipt exact-SHA CI, and retain the final tested/evidence/done
   digest chain without another source edit.

The I4 artifact intentionally carries no `netbox-go-evidence-v2` promotion
marker unless a separately reviewed future traceability packet authorizes one.
I4 closes only its bounded password/session child; the parent and
`identity-extension-pending` verification remain open.

## Local source-candidate exit conditions after activation

The checked conditions below record the pre-governance local result above.
Because this governance update changes owned source, the complete
governance-inclusive exact-byte ladder must pass before the candidate commit.

- [x] The reachable accepted-I3-based red-first shell/test revision discovers
      all twelve exact matrices and fails for their intended semantic gaps.
- [x] All twelve exact matrices pass on the rebased candidate.
- [x] Browser-origin password change rotates one current session and invalidates
      all old sessions atomically.
- [x] REST Token and gRPC bearer password change invalidate all browser sessions
      without a browser result or cookie mutation.
- [x] Current-password mismatch, malformed verifier, stale transactional state,
      session causes, CSRF, and infrastructure failures have the exact declared
      classifications.
- [x] All rollback rows prove old password/prior sessions survive and no result
      secret escapes.
- [x] A deferred PostgreSQL constraint trigger proves actual browser/Token
      COMMIT errors return absent results and roll back every DML effect.
- [x] Real PostgreSQL concurrency proves one password-change winner and both
      login/password-change lock orders using observed advisory-lock contention
      and no sleeps.
- [x] API-token behavior, I1/I2/I3 behavior, route/proto shape, and password
      policy remain unchanged.
- [x] Affected non-race/race, backend, root, link, diff, and complete L4 gates
      pass on one unchanged source digest.
- [x] Scope contains only phase-permitted paths and no generated/schema/profile/
      traceability/tier claim changed.
- [x] Parent, V1, T2/T3/T4, profile publication, deployment, production, and
      full-rewrite claims remain open.

The governance-inclusive exact-byte rerun of this checked local result
authorizes the exact candidate commit and push; it does not place I4 at
`evidence`.

## Post-push evidence-retention boundary

Green candidate exact-SHA CI is only the prerequisite for the reviewed
nine-path claim-only transition. That transition must retain the complete
tested-to-attestation manifests and diffs, pass pinned `make check`, and receive
green exact-SHA CI of its own before I4 becomes `evidence`. Its later
digest-excluded receipt records that result without changing source. Explicit
owner acceptance and `evidence -> done` remain a separate reviewed nine-path
claim/receipt sequence with their own manifest review, `make check`, and
exact-SHA CI boundaries. None of those external facts is a local
source-candidate checkbox.

## Residual work after I4

I4 deliberately leaves these items visible:

- exact password-validator, algorithm, parameter, version, bcrypt-limit,
  rehash, and imported-hash policy;
- durable login and token-creation throttling;
- trusted-proxy/client-source resolution;
- gRPC streaming authentication;
- Django-style Origin/HTTPS-Referer and masked-CSRF handling;
- final stale-cookie and broader session-lifetime policy;
- aggregate all-transport identity evidence;
- administrator CLI/RBAC/secret closeout in V1-03;
- strict REST T2, gRPC T3, real-browser T4, V1 retention, deployment, and
  production evidence.

These residuals must not be hidden behind I4's bounded session-invalidation
result or used to promote the parent credential matrix.
