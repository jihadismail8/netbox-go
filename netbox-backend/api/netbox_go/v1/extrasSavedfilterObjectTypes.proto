syntax = "proto3";

package api.netbox_go.v1;

import "api/types/types.proto";
import "google/api/annotations.proto";
import "protoc-gen-openapiv2/options/annotations.proto";
import "tagger/tagger.proto";
import "validate/validate.proto";

option go_package = "netbox-go/api/netbox_go/v1;v1";

/*
Default settings for generating *.swagger.json documents. For reference, see: https://bit.ly/4dE5jj7
Tip: To enhance the generated Swagger documentation, you can add the openapiv2_operation option to your RPC method. For example:
    option (grpc.gateway.protoc_gen_openapiv2.options.openapiv2_operation) = {
      summary: "get user by id",
      description: "get user by id",
      security: {
        security_requirement: {
          key: "BearerAuth";
          value: {}
        }
      }
    };
*/
option (grpc.gateway.protoc_gen_openapiv2.options.openapiv2_swagger) = {
  host: "localhost:8080"
  base_path: ""
  info: {
    title: "netbox_go api docs";
    version: "v1.0.0";
  }
  schemes: HTTP;
  schemes: HTTPS;
  consumes: "application/json";
  produces: "application/json";
  security_definitions: {
    security: {
      key: "BearerAuth";
      value: {
        type: TYPE_API_KEY;
        in: IN_HEADER;
        name: "Authorization";
        description: "Type Bearer your-jwt-token to Value";
      }
    }
  }
};

service extrasSavedfilterObjectTypes {
  // Create a new extrasSavedfilterObjectTypes
  rpc Create(CreateExtrasSavedfilterObjectTypesRequest) returns (CreateExtrasSavedfilterObjectTypesReply) {
    option (google.api.http) = {
      post: "/api/extras/extrasSavedfilterObjectTypes"
      body: "*"
    };
  }

  // Delete a extrasSavedfilterObjectTypes by id
  rpc DeleteByID(DeleteExtrasSavedfilterObjectTypesByIDRequest) returns (DeleteExtrasSavedfilterObjectTypesByIDReply) {
    option (google.api.http) = {
      delete: "/api/extras/extrasSavedfilterObjectTypes/{id}"
    };
  }

  // Update a extrasSavedfilterObjectTypes by id
  rpc UpdateByID(UpdateExtrasSavedfilterObjectTypesByIDRequest) returns (UpdateExtrasSavedfilterObjectTypesByIDReply) {
    option (google.api.http) = {
      put: "/api/extras/extrasSavedfilterObjectTypes/{id}"
      body: "*"
    };
  }

  // Get a extrasSavedfilterObjectTypes by id
  rpc GetByID(GetExtrasSavedfilterObjectTypesByIDRequest) returns (GetExtrasSavedfilterObjectTypesByIDReply) {
    option (google.api.http) = {
      get: "/api/extras/extrasSavedfilterObjectTypes/{id}"
    };
  }

  // Get a paginated list of extrasSavedfilterObjectTypes by custom conditions
  rpc List(ListExtrasSavedfilterObjectTypesRequest) returns (ListExtrasSavedfilterObjectTypesReply) {
    option (google.api.http) = {
      post: "/api/extras/extrasSavedfilterObjectTypes/list"
      body: "*"
    };
  }

  // Batch delete extrasSavedfilterObjectTypes by ids
  rpc DeleteByIDs(DeleteExtrasSavedfilterObjectTypesByIDsRequest) returns (DeleteExtrasSavedfilterObjectTypesByIDsReply) {
    option (google.api.http) = {
      post: "/api/extras/extrasSavedfilterObjectTypes/delete/ids"
      body: "*"
    };
  }

  // Get a extrasSavedfilterObjectTypes by custom condition
  rpc GetByCondition(GetExtrasSavedfilterObjectTypesByConditionRequest) returns (GetExtrasSavedfilterObjectTypesByConditionReply) {
    option (google.api.http) = {
      post: "/api/extras/extrasSavedfilterObjectTypes/condition"
      body: "*"
    };
  }

  // Batch get extrasSavedfilterObjectTypes by ids
  rpc ListByIDs(ListExtrasSavedfilterObjectTypesByIDsRequest) returns (ListExtrasSavedfilterObjectTypesByIDsReply) {
    option (google.api.http) = {
      post: "/api/extras/extrasSavedfilterObjectTypes/list/ids"
      body: "*"
    };
  }

  // Get a paginated list of extrasSavedfilterObjectTypes by last id
  rpc ListByLastID(ListExtrasSavedfilterObjectTypesByLastIDRequest) returns (ListExtrasSavedfilterObjectTypesByLastIDReply) {
    option (google.api.http) = {
      get: "/api/extras/extrasSavedfilterObjectTypes"
    };
  }
}


/*
Notes for defining message fields:
    1. Suggest using camel case style naming for message field names, such as firstName, lastName, etc.
    2. If the message field name ending in 'id', it is recommended to use xxxID naming format, such as userID, orderID, etc.
    3. Add validate rules https://github.com/envoyproxy/protoc-gen-validate#constraint-rules, such as:
        uint64 id = 1 [(validate.rules).uint64.gte  = 1];

If used to generate code that supports the HTTP protocol, notes for defining message fields:
    1. If the route contains the path parameter, such as /api/v1/extrasSavedfilterObjectTypes/{id}, the defined
        message must contain the name of the path parameter and the name should be added
        with a new tag, such as int64 id = 1 [(tagger.tags) = "uri:\"id\""];
    2. If the request url is followed by a query parameter, such as /api/v1/getExtrasSavedfilterObjectTypes?name=Tom,
        a form tag must be added when defining the query parameter in the message, such as:
        string name = 1 [(tagger.tags) = "form:\"name\""].
    3. When the message fields use snake_case naming (e.g., order_id), the generated swagger.json file
        will use camelCase (e.g., orderId) instead of the expected snake_case. This behavior aligns with
        the JSON tag names used by gRPC, but it can cause the Gin framework to fail to correctly bind and
        retrieve parameter values. There are two ways to resolve this issue:
            (1) Explicitly specify the JSON tag name using the json_name option, such as:
                 string order_id = 1 [json_name = "order_id"];
            (2) If you want to switch to camelCase naming and update the JSON tag name accordingly, such as:
                 string order_id = 1 [json_name = "orderID", (tagger.tags) = "json:\"orderID\""];
*/


message CreateExtrasSavedfilterObjectTypesRequest {
	int64 savedfilterID = 1; 
	int32 contenttypeID = 2; 
}

message CreateExtrasSavedfilterObjectTypesReply {
  uint64 id = 1;
}

message DeleteExtrasSavedfilterObjectTypesByIDRequest {
  uint64 id =1 [(validate.rules).uint64.gt = 0, (tagger.tags) = "uri:\"id\""];
}

message DeleteExtrasSavedfilterObjectTypesByIDReply {

}

message UpdateExtrasSavedfilterObjectTypesByIDRequest {
	uint64 id = 1 [(validate.rules).uint64.gt = 0, (tagger.tags) = "uri:\"id\""]; 
	int64 savedfilterID = 2; 
	int32 contenttypeID = 3; 
}

message UpdateExtrasSavedfilterObjectTypesByIDReply {

}

message ExtrasSavedfilterObjectTypes {
	uint64 id = 1; 
	int64 savedfilterID = 2; 
	int32 contenttypeID = 3; 
}

message GetExtrasSavedfilterObjectTypesByIDRequest {
  uint64 id =1 [(validate.rules).uint64.gt = 0, (tagger.tags) = "uri:\"id\"" ];
}

message GetExtrasSavedfilterObjectTypesByIDReply {
  ExtrasSavedfilterObjectTypes extrasSavedfilterObjectTypes = 1;
}

message ListExtrasSavedfilterObjectTypesRequest {
  api.types.Params params = 1;
}

message ListExtrasSavedfilterObjectTypesReply {
  int64 total = 1;
  repeated ExtrasSavedfilterObjectTypes extrasSavedfilterObjectTypess = 2;
}

message DeleteExtrasSavedfilterObjectTypesByIDsRequest {
  repeated uint64 ids = 1 [(validate.rules).repeated.min_items = 1];
}

message DeleteExtrasSavedfilterObjectTypesByIDsReply {

}

message GetExtrasSavedfilterObjectTypesByConditionRequest {
  types.Conditions conditions = 1;
}

message GetExtrasSavedfilterObjectTypesByConditionReply {
  ExtrasSavedfilterObjectTypes extrasSavedfilterObjectTypes = 1;
}

message ListExtrasSavedfilterObjectTypesByIDsRequest {
  repeated uint64 ids = 1 [(validate.rules).repeated.min_items = 1];
}

message ListExtrasSavedfilterObjectTypesByIDsReply {
  repeated ExtrasSavedfilterObjectTypes extrasSavedfilterObjectTypess = 1;
}

message ListExtrasSavedfilterObjectTypesByLastIDRequest {
  uint64 lastID = 1 [(tagger.tags) = "form:\"lastID\""]; // last id
  uint32 limit = 2 [(validate.rules).uint32.gt = 0, (tagger.tags) = "form:\"limit\""]; // limit size per page
  string sort = 3 [(tagger.tags) = "form:\"sort\""]; // sort by column name of table, default is -id, the - sign indicates descending order.
}

message ListExtrasSavedfilterObjectTypesByLastIDReply {
  repeated ExtrasSavedfilterObjectTypes extrasSavedfilterObjectTypess = 1;
}


