syntax = "proto3";

package api.netbox_go.v1;

import "api/types/types.proto";
import "google/api/annotations.proto";
import "protoc-gen-openapiv2/options/annotations.proto";
import "tagger/tagger.proto";
import "validate/validate.proto";

option go_package = "netbox-go/api/netbox_go/v1;v1";

/*
Default settings for generating *.swagger.json documents. For reference, see: https://bit.ly/4dE5jj7
Tip: To enhance the generated Swagger documentation, you can add the openapiv2_operation option to your RPC method. For example:
    option (grpc.gateway.protoc_gen_openapiv2.options.openapiv2_operation) = {
      summary: "get user by id",
      description: "get user by id",
      security: {
        security_requirement: {
          key: "BearerAuth";
          value: {}
        }
      }
    };
*/
option (grpc.gateway.protoc_gen_openapiv2.options.openapiv2_swagger) = {
  host: "localhost:8080"
  base_path: ""
  info: {
    title: "netbox_go api docs";
    version: "v1.0.0";
  }
  schemes: HTTP;
  schemes: HTTPS;
  consumes: "application/json";
  produces: "application/json";
  security_definitions: {
    security: {
      key: "BearerAuth";
      value: {
        type: TYPE_API_KEY;
        in: IN_HEADER;
        name: "Authorization";
        description: "Type Bearer your-jwt-token to Value";
      }
    }
  }
};

service vpnL2Vpntermination {
  // Create a new vpnL2Vpntermination
  rpc Create(CreateVpnL2VpnterminationRequest) returns (CreateVpnL2VpnterminationReply) {
    option (google.api.http) = {
      post: "/api/vpn/l2vpnterminations"
      body: "*"
    };
  }

  // Delete a vpnL2Vpntermination by id
  rpc DeleteByID(DeleteVpnL2VpnterminationByIDRequest) returns (DeleteVpnL2VpnterminationByIDReply) {
    option (google.api.http) = {
      delete: "/api/vpn/l2vpnterminations/{id}"
    };
  }

  // Update a vpnL2Vpntermination by id
  rpc UpdateByID(UpdateVpnL2VpnterminationByIDRequest) returns (UpdateVpnL2VpnterminationByIDReply) {
    option (google.api.http) = {
      put: "/api/vpn/l2vpnterminations/{id}"
      body: "*"
    };
  }

  // Get a vpnL2Vpntermination by id
  rpc GetByID(GetVpnL2VpnterminationByIDRequest) returns (GetVpnL2VpnterminationByIDReply) {
    option (google.api.http) = {
      get: "/api/vpn/l2vpnterminations/{id}"
    };
  }

  // Get a paginated list of vpnL2Vpntermination by custom conditions
  rpc List(ListVpnL2VpnterminationRequest) returns (ListVpnL2VpnterminationReply) {
    option (google.api.http) = {
      post: "/api/vpn/l2vpnterminations/list"
      body: "*"
    };
  }

  // Batch delete vpnL2Vpntermination by ids
  rpc DeleteByIDs(DeleteVpnL2VpnterminationByIDsRequest) returns (DeleteVpnL2VpnterminationByIDsReply) {
    option (google.api.http) = {
      post: "/api/vpn/l2vpnterminations/delete/ids"
      body: "*"
    };
  }

  // Get a vpnL2Vpntermination by custom condition
  rpc GetByCondition(GetVpnL2VpnterminationByConditionRequest) returns (GetVpnL2VpnterminationByConditionReply) {
    option (google.api.http) = {
      post: "/api/vpn/l2vpnterminations/condition"
      body: "*"
    };
  }

  // Batch get vpnL2Vpntermination by ids
  rpc ListByIDs(ListVpnL2VpnterminationByIDsRequest) returns (ListVpnL2VpnterminationByIDsReply) {
    option (google.api.http) = {
      post: "/api/vpn/l2vpnterminations/list/ids"
      body: "*"
    };
  }

  // Get a paginated list of vpnL2Vpntermination by last id
  rpc ListByLastID(ListVpnL2VpnterminationByLastIDRequest) returns (ListVpnL2VpnterminationByLastIDReply) {
    option (google.api.http) = {
      get: "/api/vpn/l2vpnterminations"
    };
  }
}


/*
Notes for defining message fields:
    1. Suggest using camel case style naming for message field names, such as firstName, lastName, etc.
    2. If the message field name ending in 'id', it is recommended to use xxxID naming format, such as userID, orderID, etc.
    3. Add validate rules https://github.com/envoyproxy/protoc-gen-validate#constraint-rules, such as:
        uint64 id = 1 [(validate.rules).uint64.gte  = 1];

If used to generate code that supports the HTTP protocol, notes for defining message fields:
    1. If the route contains the path parameter, such as /api/v1/vpnL2Vpntermination/{id}, the defined
        message must contain the name of the path parameter and the name should be added
        with a new tag, such as int64 id = 1 [(tagger.tags) = "uri:\"id\""];
    2. If the request url is followed by a query parameter, such as /api/v1/getVpnL2Vpntermination?name=Tom,
        a form tag must be added when defining the query parameter in the message, such as:
        string name = 1 [(tagger.tags) = "form:\"name\""].
    3. When the message fields use snake_case naming (e.g., order_id), the generated swagger.json file
        will use camelCase (e.g., orderId) instead of the expected snake_case. This behavior aligns with
        the JSON tag names used by gRPC, but it can cause the Gin framework to fail to correctly bind and
        retrieve parameter values. There are two ways to resolve this issue:
            (1) Explicitly specify the JSON tag name using the json_name option, such as:
                 string order_id = 1 [json_name = "order_id"];
            (2) If you want to switch to camelCase naming and update the JSON tag name accordingly, such as:
                 string order_id = 1 [json_name = "orderID", (tagger.tags) = "json:\"orderID\""];
*/


message CreateVpnL2VpnterminationRequest {
	string created = 1; 
	string lastUpdated = 2; 
	string customFieldData = 3; 
	int64 assignedObjectID = 4; 
	int32 assignedObjectTypeID = 5; 
	int64 l2vpnID = 6; 
}

message CreateVpnL2VpnterminationReply {
  uint64 id = 1;
}

message DeleteVpnL2VpnterminationByIDRequest {
  uint64 id =1 [(validate.rules).uint64.gt = 0, (tagger.tags) = "uri:\"id\""];
}

message DeleteVpnL2VpnterminationByIDReply {

}

message UpdateVpnL2VpnterminationByIDRequest {
	uint64 id = 1 [(validate.rules).uint64.gt = 0, (tagger.tags) = "uri:\"id\""]; 
	string created = 2; 
	string lastUpdated = 3; 
	string customFieldData = 4; 
	int64 assignedObjectID = 5; 
	int32 assignedObjectTypeID = 6; 
	int64 l2vpnID = 7; 
}

message UpdateVpnL2VpnterminationByIDReply {

}

message VpnL2Vpntermination {
	uint64 id = 1; 
	string created = 2; 
	string lastUpdated = 3; 
	string customFieldData = 4; 
	int64 assignedObjectID = 5; 
	int32 assignedObjectTypeID = 6; 
	int64 l2vpnID = 7; 
}

message GetVpnL2VpnterminationByIDRequest {
  uint64 id =1 [(validate.rules).uint64.gt = 0, (tagger.tags) = "uri:\"id\"" ];
}

message GetVpnL2VpnterminationByIDReply {
  VpnL2Vpntermination vpnL2Vpntermination = 1;
}

message ListVpnL2VpnterminationRequest {
  api.types.Params params = 1;
}

message ListVpnL2VpnterminationReply {
  int64 total = 1;
  repeated VpnL2Vpntermination vpnL2Vpnterminations = 2;
}

message DeleteVpnL2VpnterminationByIDsRequest {
  repeated uint64 ids = 1 [(validate.rules).repeated.min_items = 1];
}

message DeleteVpnL2VpnterminationByIDsReply {

}

message GetVpnL2VpnterminationByConditionRequest {
  types.Conditions conditions = 1;
}

message GetVpnL2VpnterminationByConditionReply {
  VpnL2Vpntermination vpnL2Vpntermination = 1;
}

message ListVpnL2VpnterminationByIDsRequest {
  repeated uint64 ids = 1 [(validate.rules).repeated.min_items = 1];
}

message ListVpnL2VpnterminationByIDsReply {
  repeated VpnL2Vpntermination vpnL2Vpnterminations = 1;
}

message ListVpnL2VpnterminationByLastIDRequest {
  uint64 lastID = 1 [(tagger.tags) = "form:\"lastID\""]; // last id
  uint32 limit = 2 [(validate.rules).uint32.gt = 0, (tagger.tags) = "form:\"limit\""]; // limit size per page
  string sort = 3 [(tagger.tags) = "form:\"sort\""]; // sort by column name of table, default is -id, the - sign indicates descending order.
}

message ListVpnL2VpnterminationByLastIDReply {
  repeated VpnL2Vpntermination vpnL2Vpnterminations = 1;
}


