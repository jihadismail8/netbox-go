// Code generated by https://github.com/go-dev-frame/sponge
// Test_service_extrasConfigcontextprofile_methods is used to test the extrasConfigcontextprofile api
// Test_service_extrasConfigcontextprofile_benchmark is used to performance test the extrasConfigcontextprofile api

package service

import (
	"context"
	"encoding/json"
	"fmt"
	"testing"
	"time"

	"github.com/go-dev-frame/sponge/pkg/grpc/benchmark"

	netbox_goV1 "netbox-go/api/netbox_go/v1"
	"netbox-go/configs"
	"netbox-go/internal/config"
)

// Test service extrasConfigcontextprofile api via grpc client
func Test_service_extrasConfigcontextprofile_methods(t *testing.T) {
	conn := getRPCClientConnForTest()
	cli := netbox_goV1.NewExtrasConfigcontextprofileClient(conn)
	ctx, _ := context.WithTimeout(context.Background(), time.Second*30)

	tests := []struct {
		name    string
		fn      func() (interface{}, error)
		wantErr bool
	}{

		{
			name: "Create",
			fn: func() (interface{}, error) {
				// todo type in the parameters before testing
				req := &netbox_goV1.CreateExtrasConfigcontextprofileRequest{
					Created:         "",
					LastUpdated:     "",
					CustomFieldData: "",
					DataPath:        "",
					AutoSyncEnabled: false,
					DataSynced:      "",
					Comments:        "",
					Name:            "",
					Description:     "",
					Schema:          "",
					DataFileID:      0,
					DataSourceID:    0,
				}

				return cli.Create(ctx, req)
			},
			wantErr: false,
		},

		{
			name: "DeleteByID",
			fn: func() (interface{}, error) {
				// todo type in the parameters before testing
				req := &netbox_goV1.DeleteExtrasConfigcontextprofileByIDRequest{
					Id: 0,
				}

				return cli.DeleteByID(ctx, req)
			},
			wantErr: false,
		},

		{
			name: "UpdateByID",
			fn: func() (interface{}, error) {
				// todo type in the parameters before testing
				req := &netbox_goV1.UpdateExtrasConfigcontextprofileByIDRequest{
					Id:              0,
					Created:         "",
					LastUpdated:     "",
					CustomFieldData: "",
					DataPath:        "",
					AutoSyncEnabled: false,
					DataSynced:      "",
					Comments:        "",
					Name:            "",
					Description:     "",
					Schema:          "",
					DataFileID:      0,
					DataSourceID:    0,
				}

				return cli.UpdateByID(ctx, req)
			},
			wantErr: false,
		},

		{
			name: "GetByID",
			fn: func() (interface{}, error) {
				// todo type in the parameters before testing
				req := &netbox_goV1.GetExtrasConfigcontextprofileByIDRequest{
					Id: 0,
				}

				return cli.GetByID(ctx, req)
			},
			wantErr: false,
		},

		{
			name: "List",
			fn: func() (interface{}, error) {
				// todo type in the parameters before testing
				req := &netbox_goV1.ListExtrasConfigcontextprofileRequest{
					Params: nil,
				}

				return cli.List(ctx, req)
			},
			wantErr: false,
		},

		{
			name: "DeleteByIDs",
			fn: func() (interface{}, error) {
				// todo type in the parameters before testing
				req := &netbox_goV1.DeleteExtrasConfigcontextprofileByIDsRequest{
					Ids: nil,
				}

				return cli.DeleteByIDs(ctx, req)
			},
			wantErr: false,
		},

		{
			name: "GetByCondition",
			fn: func() (interface{}, error) {
				// todo type in the parameters before testing
				req := &netbox_goV1.GetExtrasConfigcontextprofileByConditionRequest{
					Conditions: nil,
				}

				return cli.GetByCondition(ctx, req)
			},
			wantErr: false,
		},

		{
			name: "ListByIDs",
			fn: func() (interface{}, error) {
				// todo type in the parameters before testing
				req := &netbox_goV1.ListExtrasConfigcontextprofileByIDsRequest{
					Ids: nil,
				}

				return cli.ListByIDs(ctx, req)
			},
			wantErr: false,
		},

		{
			name: "ListByLastID",
			fn: func() (interface{}, error) {
				// todo type in the parameters before testing
				req := &netbox_goV1.ListExtrasConfigcontextprofileByLastIDRequest{
					LastID: 0,  // last id
					Limit:  0,  // limit size per page
					Sort:   "", // sort by column name of table, default is -id, the - sign indicates descending order.
				}

				return cli.ListByLastID(ctx, req)
			},
			wantErr: false,
		},
	}

	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, err := tt.fn()
			if (err != nil) != tt.wantErr {
				t.Errorf("test '%s' error = %v, wantErr %v", tt.name, err, tt.wantErr)
				return
			}
			data, _ := json.MarshalIndent(got, "", "    ")
			fmt.Println(string(data))
		})
	}
}

// performance test service extrasConfigcontextprofile api, copy the report to
// the browser to view when the pressure test is finished.
func Test_service_extrasConfigcontextprofile_benchmark(t *testing.T) {
	err := config.Init(configs.Path("netbox_go.yml"))
	if err != nil {
		panic(err)
	}

	grpcClientCfg := getGRPCClientCfg()
	host := fmt.Sprintf("%s:%d", grpcClientCfg.Host, grpcClientCfg.Port)
	protoFile := configs.Path("../api/netbox_go/v1/extrasConfigcontextprofile.proto")
	// If third-party dependencies are missing during the press test,
	// copy them to the project's third_party directory.
	dependentProtoFilePath := []string{
		configs.Path("../third_party"), // third_party directory
		configs.Path(".."),             // Previous level of third_party
	}

	tests := []struct {
		name    string
		fn      func() error
		wantErr bool
	}{

		{
			name: "Create",
			fn: func() error {
				// todo type in the parameters before benchmark testing
				message := &netbox_goV1.CreateExtrasConfigcontextprofileRequest{
					Created:         "",
					LastUpdated:     "",
					CustomFieldData: "",
					DataPath:        "",
					AutoSyncEnabled: false,
					DataSynced:      "",
					Comments:        "",
					Name:            "",
					Description:     "",
					Schema:          "",
					DataFileID:      0,
					DataSourceID:    0,
				}
				total := 1000 // total number of requests

				b, err := benchmark.New(host, protoFile, "Create", message, dependentProtoFilePath, total)
				if err != nil {
					return err
				}
				return b.Run()
			},
			wantErr: false,
		},

		{
			name: "DeleteByID",
			fn: func() error {
				// todo type in the parameters before benchmark testing
				message := &netbox_goV1.DeleteExtrasConfigcontextprofileByIDRequest{
					Id: 0,
				}
				total := 1000 // total number of requests

				b, err := benchmark.New(host, protoFile, "DeleteByID", message, dependentProtoFilePath, total)
				if err != nil {
					return err
				}
				return b.Run()
			},
			wantErr: false,
		},

		{
			name: "UpdateByID",
			fn: func() error {
				// todo type in the parameters before benchmark testing
				message := &netbox_goV1.UpdateExtrasConfigcontextprofileByIDRequest{
					Id:              0,
					Created:         "",
					LastUpdated:     "",
					CustomFieldData: "",
					DataPath:        "",
					AutoSyncEnabled: false,
					DataSynced:      "",
					Comments:        "",
					Name:            "",
					Description:     "",
					Schema:          "",
					DataFileID:      0,
					DataSourceID:    0,
				}
				total := 1000 // total number of requests

				b, err := benchmark.New(host, protoFile, "UpdateByID", message, dependentProtoFilePath, total)
				if err != nil {
					return err
				}
				return b.Run()
			},
			wantErr: false,
		},

		{
			name: "GetByID",
			fn: func() error {
				// todo type in the parameters before benchmark testing
				message := &netbox_goV1.GetExtrasConfigcontextprofileByIDRequest{
					Id: 0,
				}
				total := 1000 // total number of requests

				b, err := benchmark.New(host, protoFile, "GetByID", message, dependentProtoFilePath, total)
				if err != nil {
					return err
				}
				return b.Run()
			},
			wantErr: false,
		},

		{
			name: "List",
			fn: func() error {
				// todo type in the parameters before benchmark testing
				message := &netbox_goV1.ListExtrasConfigcontextprofileRequest{
					Params: nil,
				}
				total := 1000 // total number of requests

				b, err := benchmark.New(host, protoFile, "List", message, dependentProtoFilePath, total)
				if err != nil {
					return err
				}
				return b.Run()
			},
			wantErr: false,
		},

		{
			name: "DeleteByIDs",
			fn: func() error {
				// todo type in the parameters before benchmark testing
				message := &netbox_goV1.DeleteExtrasConfigcontextprofileByIDsRequest{
					Ids: nil,
				}
				total := 1000 // total number of requests

				b, err := benchmark.New(host, protoFile, "DeleteByIDs", message, dependentProtoFilePath, total)
				if err != nil {
					return err
				}
				return b.Run()
			},
			wantErr: false,
		},

		{
			name: "GetByCondition",
			fn: func() error {
				// todo type in the parameters before benchmark testing
				message := &netbox_goV1.GetExtrasConfigcontextprofileByConditionRequest{
					Conditions: nil,
				}
				total := 1000 // total number of requests

				b, err := benchmark.New(host, protoFile, "GetByCondition", message, dependentProtoFilePath, total)
				if err != nil {
					return err
				}
				return b.Run()
			},
			wantErr: false,
		},

		{
			name: "ListByIDs",
			fn: func() error {
				// todo type in the parameters before benchmark testing
				message := &netbox_goV1.ListExtrasConfigcontextprofileByIDsRequest{
					Ids: nil,
				}
				total := 1000 // total number of requests

				b, err := benchmark.New(host, protoFile, "ListByIDs", message, dependentProtoFilePath, total)
				if err != nil {
					return err
				}
				return b.Run()
			},
			wantErr: false,
		},

		{
			name: "ListByLastID",
			fn: func() error {
				// todo type in the parameters before benchmark testing
				message := &netbox_goV1.ListExtrasConfigcontextprofileByLastIDRequest{
					LastID: 0,  // last id
					Limit:  0,  // limit size per page
					Sort:   "", // sort by column name of table, default is -id, the - sign indicates descending order.
				}
				total := 1000 // total number of requests

				b, err := benchmark.New(host, protoFile, "ListByLastID", message, dependentProtoFilePath, total)
				if err != nil {
					return err
				}
				return b.Run()
			},
			wantErr: false,
		},
	}

	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			err := tt.fn()
			if (err != nil) != tt.wantErr {
				t.Errorf("test '%s' error = %v, wantErr %v", tt.name, err, tt.wantErr)
				return
			}
		})
	}
}
