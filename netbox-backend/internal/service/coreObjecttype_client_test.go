// Code generated by https://github.com/go-dev-frame/sponge
// Test_service_coreObjecttype_methods is used to test the coreObjecttype api
// Test_service_coreObjecttype_benchmark is used to performance test the coreObjecttype api

package service

import (
	"context"
	"encoding/json"
	"fmt"
	"testing"
	"time"

	"github.com/go-dev-frame/sponge/pkg/grpc/benchmark"

	netbox_goV1 "netbox-go/api/netbox_go/v1"
	"netbox-go/configs"
	"netbox-go/internal/config"
)

// Test service coreObjecttype api via grpc client
func Test_service_coreObjecttype_methods(t *testing.T) {
	conn := getRPCClientConnForTest()
	cli := netbox_goV1.NewCoreObjecttypeClient(conn)
	ctx, _ := context.WithTimeout(context.Background(), time.Second*30)

	tests := []struct {
		name    string
		fn      func() (interface{}, error)
		wantErr bool
	}{

		{
			name: "Create",
			fn: func() (interface{}, error) {
				// todo type in the parameters before testing
				req := &netbox_goV1.CreateCoreObjecttypeRequest{
					ContenttypePtrID: 0,
					Public:           false,
					Features:         "",
				}

				return cli.Create(ctx, req)
			},
			wantErr: false,
		},

		{
			name: "DeleteByContenttypePtrID",
			fn: func() (interface{}, error) {
				// todo type in the parameters before testing
				req := &netbox_goV1.DeleteCoreObjecttypeByContenttypePtrIDRequest{
					ContenttypePtrID: 0,
				}

				return cli.DeleteByContenttypePtrID(ctx, req)
			},
			wantErr: false,
		},

		{
			name: "UpdateByContenttypePtrID",
			fn: func() (interface{}, error) {
				// todo type in the parameters before testing
				req := &netbox_goV1.UpdateCoreObjecttypeByContenttypePtrIDRequest{
					ContenttypePtrID: 0,
					Public:           false,
					Features:         "",
				}

				return cli.UpdateByContenttypePtrID(ctx, req)
			},
			wantErr: false,
		},

		{
			name: "GetByContenttypePtrID",
			fn: func() (interface{}, error) {
				// todo type in the parameters before testing
				req := &netbox_goV1.GetCoreObjecttypeByContenttypePtrIDRequest{
					ContenttypePtrID: 0,
				}

				return cli.GetByContenttypePtrID(ctx, req)
			},
			wantErr: false,
		},

		{
			name: "List",
			fn: func() (interface{}, error) {
				// todo type in the parameters before testing
				req := &netbox_goV1.ListCoreObjecttypeRequest{
					Params: nil,
				}

				return cli.List(ctx, req)
			},
			wantErr: false,
		},

		{
			name: "DeleteByContenttypePtrIDs",
			fn: func() (interface{}, error) {
				// todo type in the parameters before testing
				req := &netbox_goV1.DeleteCoreObjecttypeByContenttypePtrIDsRequest{
					ContenttypePtrIDs: nil,
				}

				return cli.DeleteByContenttypePtrIDs(ctx, req)
			},
			wantErr: false,
		},

		{
			name: "GetByCondition",
			fn: func() (interface{}, error) {
				// todo type in the parameters before testing
				req := &netbox_goV1.GetCoreObjecttypeByConditionRequest{
					Conditions: nil,
				}

				return cli.GetByCondition(ctx, req)
			},
			wantErr: false,
		},

		{
			name: "ListByContenttypePtrIDs",
			fn: func() (interface{}, error) {
				// todo type in the parameters before testing
				req := &netbox_goV1.ListCoreObjecttypeByContenttypePtrIDsRequest{
					ContenttypePtrIDs: nil,
				}

				return cli.ListByContenttypePtrIDs(ctx, req)
			},
			wantErr: false,
		},

		{
			name: "ListByLastContenttypePtrID",
			fn: func() (interface{}, error) {
				// todo type in the parameters before testing
				req := &netbox_goV1.ListCoreObjecttypeByLastContenttypePtrIDRequest{
					LastContenttypePtrID: 0,
					Limit:                0,  // limit size per page
					Sort:                 "", // sort by column name of table, default is -contenttype_ptr_id, the - sign indicates descending order.
				}

				return cli.ListByLastContenttypePtrID(ctx, req)
			},
			wantErr: false,
		},
	}

	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, err := tt.fn()
			if (err != nil) != tt.wantErr {
				t.Errorf("test '%s' error = %v, wantErr %v", tt.name, err, tt.wantErr)
				return
			}
			data, _ := json.MarshalIndent(got, "", "    ")
			fmt.Println(string(data))
		})
	}
}

// performance test service coreObjecttype api, copy the report to
// the browser to view when the pressure test is finished.
func Test_service_coreObjecttype_benchmark(t *testing.T) {
	err := config.Init(configs.Path("netbox_go.yml"))
	if err != nil {
		panic(err)
	}

	grpcClientCfg := getGRPCClientCfg()
	host := fmt.Sprintf("%s:%d", grpcClientCfg.Host, grpcClientCfg.Port)
	protoFile := configs.Path("../api/netbox_go/v1/coreObjecttype.proto")
	// If third-party dependencies are missing during the press test,
	// copy them to the project's third_party directory.
	dependentProtoFilePath := []string{
		configs.Path("../third_party"), // third_party directory
		configs.Path(".."),             // Previous level of third_party
	}

	tests := []struct {
		name    string
		fn      func() error
		wantErr bool
	}{

		{
			name: "Create",
			fn: func() error {
				// todo type in the parameters before benchmark testing
				message := &netbox_goV1.CreateCoreObjecttypeRequest{
					ContenttypePtrID: 0,
					Public:           false,
					Features:         "",
				}
				total := 1000 // total number of requests

				b, err := benchmark.New(host, protoFile, "Create", message, dependentProtoFilePath, total)
				if err != nil {
					return err
				}
				return b.Run()
			},
			wantErr: false,
		},

		{
			name: "DeleteByContenttypePtrID",
			fn: func() error {
				// todo type in the parameters before benchmark testing
				message := &netbox_goV1.DeleteCoreObjecttypeByContenttypePtrIDRequest{
					ContenttypePtrID: 0,
				}
				total := 1000 // total number of requests

				b, err := benchmark.New(host, protoFile, "DeleteByContenttypePtrID", message, dependentProtoFilePath, total)
				if err != nil {
					return err
				}
				return b.Run()
			},
			wantErr: false,
		},

		{
			name: "UpdateByContenttypePtrID",
			fn: func() error {
				// todo type in the parameters before benchmark testing
				message := &netbox_goV1.UpdateCoreObjecttypeByContenttypePtrIDRequest{
					ContenttypePtrID: 0,
					Public:           false,
					Features:         "",
				}
				total := 1000 // total number of requests

				b, err := benchmark.New(host, protoFile, "UpdateByContenttypePtrID", message, dependentProtoFilePath, total)
				if err != nil {
					return err
				}
				return b.Run()
			},
			wantErr: false,
		},

		{
			name: "GetByContenttypePtrID",
			fn: func() error {
				// todo type in the parameters before benchmark testing
				message := &netbox_goV1.GetCoreObjecttypeByContenttypePtrIDRequest{
					ContenttypePtrID: 0,
				}
				total := 1000 // total number of requests

				b, err := benchmark.New(host, protoFile, "GetByContenttypePtrID", message, dependentProtoFilePath, total)
				if err != nil {
					return err
				}
				return b.Run()
			},
			wantErr: false,
		},

		{
			name: "List",
			fn: func() error {
				// todo type in the parameters before benchmark testing
				message := &netbox_goV1.ListCoreObjecttypeRequest{
					Params: nil,
				}
				total := 1000 // total number of requests

				b, err := benchmark.New(host, protoFile, "List", message, dependentProtoFilePath, total)
				if err != nil {
					return err
				}
				return b.Run()
			},
			wantErr: false,
		},

		{
			name: "DeleteByContenttypePtrIDs",
			fn: func() error {
				// todo type in the parameters before benchmark testing
				message := &netbox_goV1.DeleteCoreObjecttypeByContenttypePtrIDsRequest{
					ContenttypePtrIDs: nil,
				}
				total := 1000 // total number of requests

				b, err := benchmark.New(host, protoFile, "DeleteByContenttypePtrIDs", message, dependentProtoFilePath, total)
				if err != nil {
					return err
				}
				return b.Run()
			},
			wantErr: false,
		},

		{
			name: "GetByCondition",
			fn: func() error {
				// todo type in the parameters before benchmark testing
				message := &netbox_goV1.GetCoreObjecttypeByConditionRequest{
					Conditions: nil,
				}
				total := 1000 // total number of requests

				b, err := benchmark.New(host, protoFile, "GetByCondition", message, dependentProtoFilePath, total)
				if err != nil {
					return err
				}
				return b.Run()
			},
			wantErr: false,
		},

		{
			name: "ListByContenttypePtrIDs",
			fn: func() error {
				// todo type in the parameters before benchmark testing
				message := &netbox_goV1.ListCoreObjecttypeByContenttypePtrIDsRequest{
					ContenttypePtrIDs: nil,
				}
				total := 1000 // total number of requests

				b, err := benchmark.New(host, protoFile, "ListByContenttypePtrIDs", message, dependentProtoFilePath, total)
				if err != nil {
					return err
				}
				return b.Run()
			},
			wantErr: false,
		},

		{
			name: "ListByLastContenttypePtrID",
			fn: func() error {
				// todo type in the parameters before benchmark testing
				message := &netbox_goV1.ListCoreObjecttypeByLastContenttypePtrIDRequest{
					LastContenttypePtrID: 0,
					Limit:                0,  // limit size per page
					Sort:                 "", // sort by column name of table, default is -contenttype_ptr_id, the - sign indicates descending order.
				}
				total := 1000 // total number of requests

				b, err := benchmark.New(host, protoFile, "ListByLastContenttypePtrID", message, dependentProtoFilePath, total)
				if err != nil {
					return err
				}
				return b.Run()
			},
			wantErr: false,
		},
	}

	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			err := tt.fn()
			if (err != nil) != tt.wantErr {
				t.Errorf("test '%s' error = %v, wantErr %v", tt.name, err, tt.wantErr)
				return
			}
		})
	}
}
